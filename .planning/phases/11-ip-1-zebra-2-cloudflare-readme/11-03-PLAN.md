---
phase: 11-ip-1-zebra-2-cloudflare-readme
plan: 03
type: execute
wave: 2
depends_on: [11-01, 11-02]
files_modified:
  - .github/workflows/deploy.yml
autonomous: false
requirements:
  - DEPLOY-02
must_haves:
  truths:
    - "GitHub Actions sets DEPLOY_PLATFORM=cloudflare for Cloudflare deployment"
    - "Build step uses npm run build (single command, not build:cloudflare)"
    - "Wrangler deploys to correct dist directory (dist not ./dist/client)"
    - "Node.js 22 is used for Astro 6 compatibility"
  artifacts:
    - path: .github/workflows/deploy.yml
      provides: "Cloudflare Pages deployment CI/CD workflow"
      contains: "DEPLOY_PLATFORM: cloudflare"
      contains: "node-version: '22'"
  key_links:
    - from: ".github/workflows/deploy.yml Build step"
      to: "build.js"
      via: "npm run build command"
      pattern: "npm run build"
    - from: ".github/workflows/deploy.yml env.DEPLOY_PLATFORM"
      to: "astro.config.mjs platform detection"
      via: "Environment variable in GitHub Actions"
      pattern: "DEPLOY_PLATFORM.*cloudflare"
    - from: ".github/workflows/deploy.yml Wrangler step"
      to: "dist directory"
      via: "pages deploy dist command"
      pattern: "pages deploy dist"
---

<objective>
Update GitHub Actions deployment workflow to use the new single-build command with DEPLOY_PLATFORM environment variable for Cloudflare Pages deployment.

Purpose: Align CI/CD deployment with local development workflow (single build command) and ensure Cloudflare builds use the correct adapter.

Output: Updated .github/workflows/deploy.yml with DEPLOY_PLATFORM environment variable and corrected deployment paths.
</objective>

<execution_context>
@/Users/wenjiaqi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenjiaqi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-ip-1-zebra-2-cloudflare-readme/11-CONTEXT.md
@.planning/phases/11-ip-1-zebra-2-cloudflare-readme/11-RESEARCH.md
@.planning/phases/11-ip-1-zebra-2-cloudflare-readme/11-01-PLAN.md
@.planning/phases/11-ip-1-zebra-2-cloudflare-readme/11-02-PLAN.md
@.github/workflows/deploy.yml

<interfaces>
<!-- Current GitHub Actions deployment workflow -->

From .github/workflows/deploy.yml (current state):
```yaml
name: Deploy to Cloudflare Pages

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci --legacy-peer-deps --force

      - name: Build
        run: npm run build:cloudflare

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy dist --project-name=cool-blog
```

Known issues from previous fixes:
- Node.js 22 already applied (fixes Astro 6 compatibility)
- Wrangler command parameter already applied (fixes deprecated parameters)
- Build command still uses build:cloudflare (needs update to use DEPLOY_PLATFORM)
- Dist path is "dist" (correct for SSR builds)

After Plan 11-02:
- build:cloudflare script no longer exists
- Must use npm run build with DEPLOY_PLATFORM=cloudflare
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update GitHub Actions workflow for DEPLOY_PLATFORM</name>
  <files>.github/workflows/deploy.yml</files>
  <read_first>.github/workflows/deploy.yml</read_first>
  <action>
Update .github/workflows/deploy.yml Build step to use DEPLOY_PLATFORM environment variable:

1. Change the Build step to:
   ```yaml
   - name: Build
     env:
       DEPLOY_PLATFORM: cloudflare
     run: npm run build
   ```

2. Verify the Wrangler step uses correct dist path:
   ```yaml
   - name: Deploy to Cloudflare Pages
     uses: cloudflare/wrangler-action@v3
     with:
       apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
       accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
       command: pages deploy dist --project-name=cool-blog
   ```

3. Keep all other configuration unchanged:
   - Trigger conditions (push to master, PR to master)
   - Node.js version 22
   - npm ci --legacy-peer-deps --force
   - Wrangler action v3

DO NOT:
- Change node-version from '22' (required for Astro 6)
- Modify wrangler-action version or command structure (already fixed)
- Change the project-name from 'cool-blog'
- Add environment variables for DATABASE_URL (already configured in Cloudflare Pages)
  </action>
  <verify>
    <automated>grep -q "DEPLOY_PLATFORM: cloudflare" .github/workflows/deploy.yml && grep -q "run: npm run build" .github/workflows/deploy.yml && ! grep -q "build:cloudflare" .github/workflows/deploy.yml</automated>
  </verify>
  <done>
    .github/workflows/deploy.yml updated with:
    - Build step has env.DEPLOY_PLATFORM: cloudflare
    - Build step runs npm run build (not npm run build:cloudflare)
    - Wrangler deploys to dist directory (not ./dist/client)
    - Node.js 22 configuration preserved
    - All other configuration unchanged
  </done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Updated GitHub Actions workflow that uses DEPLOY_PLATFORM environment variable and single build command for Cloudflare Pages deployment</what-built>
  <how-to-verify>
1. Review workflow changes:
   ```bash
   cat .github/workflows/deploy.yml
   ```
   Expected:
   - Build step contains "DEPLOY_PLATFORM: cloudflare"
   - Build step runs "npm run build"
   - No reference to "build:cloudflare"

2. Test locally (simulating CI/CD environment):
   ```bash
   DEPLOY_PLATFORM=cloudflare npm ci --legacy-peer-deps --force
   DEPLOY_PLATFORM=cloudflare npm run build
   ls -la dist/
   ```
   Expected:
   - dist/_worker.js exists (Cloudflare Workers entry point)
   - dist/client/ directory exists with static assets

3. Verify workflow syntax (if act is installed):
   ```bash
   act -n push  # Dry-run workflow
   ```
   Expected: No syntax errors in workflow file

4. Optional: Trigger deployment to verify:
   ```bash
   git add .github/workflows/deploy.yml
   git commit -m "test: update deployment workflow"
   git push
   ```
   Then check GitHub Actions tab for successful deployment
  </how-to-verify>
  <resume-signal>Type "approved" if workflow is correct, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. Build step uses DEPLOY_PLATFORM=cloudflare environment variable
2. Build command is npm run build (not npm run build:cloudflare)
3. Wrangler deploys to dist directory (correct for SSR builds)
4. Node.js 22 preserved for Astro 6 compatibility
5. All other workflow configuration unchanged
</verification>

<success_criteria>
- GitHub Actions workflow successfully deploys to Cloudflare Pages
- Build uses Cloudflare adapter (verified by dist/_worker.js output)
- Single build command works in both local and CI/CD environments
</success_criteria>

<output>
After completion, create `.planning/phases/11-ip-1-zebra-2-cloudflare-readme/11-03-SUMMARY.md`
</output>
