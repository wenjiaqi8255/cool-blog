---
phase: 09-ui-ux-polish-and-content-management
plan: 03
type: execute
wave: 2
depends_on: ["00", "01", "02"]
files_modified: [src/config/content.ts, src/pages/index.astro, src/pages/articles/index.astro]
autonomous: true
requirements: [UI-06]

must_haves:
  truths:
    - "Content config file exports pages object with home, articles, portfolio keys"
    - "Each page has title and description properties"
    - "TypeScript types are exported from content.ts"
    - "index.astro imports and uses content config"
    - "articles/index.astro imports and uses content config"
  artifacts:
    - path: "src/config/content.ts"
      provides: "Page content configuration"
      exports: ["pages", "PageKey", "PageContent"]
  key_links:
    - from: "src/pages/index.astro"
      to: "src/config/content.ts"
      via: "import"
      pattern: "from.*config/content"
    - from: "src/pages/articles/index.astro"
      to: "src/config/content.ts"
      via: "import"
      pattern: "from.*config/content"
---

<objective>
Implement variable-driven content management. Create config file for page titles and descriptions.
</objective>

<execution_context>
@/Users/wenjiaqi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenjiaqi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/pages/index.astro
@src/pages/articles/index.astro
@src/config/content.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Content Config File</name>
  <files>src/config/content.ts</files>
  <read_first>src/pages/index.astro, src/pages/articles/index.astro</read_first>
  <action>
Create `src/config/content.ts` with page content configuration:

```typescript
export interface PageContent {
  title: string;
  description: string;
}

export const pages = {
  home: {
    title: 'KERNEL_PANIC / ARCHITECTURE & SYSTEMS',
    description: 'Computing as craft — Technical writing and portfolio'
  },
  articles: {
    title: 'ARTICLES | Articles',
    description: 'Deep dives into machine learning, distributed systems, and the craft of building robust software.'
  },
  portfolio: {
    title: 'PORTFOLIO | Projects',
    description: 'Visual showcase of projects and experiments'
  }
} as const;

export type PageKey = keyof typeof pages;
```

Create the directory first if it doesn't exist.
  </action>
  <acceptance_criteria>
- src/config/content.ts exists
- Exports pages object with home, articles, portfolio keys
- Each page has title and description properties
- TypeScript types are exported
  </acceptance_criteria>
  <verify>
<automated>ls src/config/content.ts && npm run test:unit -- src/config/content.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>Content config file created with page titles and descriptions</done>
</task>

<task type="auto">
  <name>Task 2: Update Pages to Use Content Config</name>
  <files>src/pages/index.astro, src/pages/articles/index.astro</files>
  <read_first>src/pages/index.astro, src/pages/articles/index.astro, src/config/content.ts</read_first>
  <action>
Update index.astro and articles/index.astro to use the content config for page titles and descriptions.

For src/pages/articles/index.astro:
1. Import: `import { pages } from '../../config/content';`
2. Replace hardcoded title with: `title={pages.articles.title}`
3. Replace hardcoded description with: `description={pages.articles.description}`

For src/pages/index.astro:
1. Import: `import { pages } from '../config/content';`
2. Replace hardcoded title with: `title={pages.home.title}`
3. Replace hardcoded description with: `description={pages.home.description}`
  </action>
  <acceptance_criteria>
- index.astro imports and uses content config
- articles/index.astro imports and uses content config
- Page titles/descriptions come from config file
  </acceptance_criteria>
<verify>
<automated>grep -E "from.*config/content" src/pages/index.astro src/pages/articles/index.astro && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Pages use variable-driven content from config file</done>
</task>

</tasks>

<verification>
- Content config file exists with page metadata
- Index and articles pages import and use config
- Tests run successfully for content config
</verification>

<success_criteria>
Page titles and descriptions are now driven by config file
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-ux-polish-and-content-management/09-03-SUMMARY.md`
</output>