---
phase: 09-ui-ux-polish-and-content-management
plan: 01
type: execute
wave: 1
depends_on: ["00"]
files_modified: [src/components/layout/Header.astro, src/styles/global.css]
autonomous: true
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "Header displays frosted glass effect when scrolling down 20px+"
    - "Header removes effect when scrolling back up"
    - "Code blocks have gray (#1f1f1f) background"
    - "Code text is black (#111111) for readability"
  artifacts:
    - path: "src/components/layout/Header.astro"
      provides: "Header with scroll-based frosted glass effect"
      contains: "scrolled class"
    - path: "src/styles/global.css"
      provides: "Code block styling"
      contains: "pre.*background.*#1f1f1f"
  key_links:
    - from: "src/components/layout/Header.astro"
      to: "src/styles/global.css"
      via: "CSS class"
      pattern: ".scrolled"
---

<objective>
Polish header and code block styling. Add frosted glass effect to header on scroll, verify code block text color matches spec (#111111 on #1f1f1f).
</objective>

<execution_context>
@/Users/wenjiaqi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenjiaqi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/layout/Header.astro
@src/styles/global.css
@src/pages/articles/[slug].astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header Frosted Glass Effect</name>
  <files>src/components/layout/Header.astro</files>
  <read_first>src/components/layout/Header.astro</read_first>
  <action>
Add scroll event listener to Header.astro that adds `.scrolled` class when scrollY > 20px:

1. Add `<script>` tag inside the component
2. Add event listener: `window.addEventListener('scroll', ...)` with passive: true
3. Toggle `.scrolled` class on header element based on scroll position

Add `.scrolled` CSS styles:
- `background: rgba(255, 255, 255, 0.8)`
- `backdrop-filter: blur(12px)`
- `-webkit-backdrop-filter: blur(12px)`
- `border-bottom: 1px solid rgba(238, 238, 238, 0.5)`
- Add transition: `transition: background 0.3s, backdrop-filter 0.3s`
  </action>
  <acceptance_criteria>
- Scroll down 20px+, header gains frosted glass effect
- Scroll back up, effect removes
- Mobile works (same behavior)
  </acceptance_criteria>
  <verify>
<automated>grep -n "scrolled" src/components/layout/Header.astro && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Header displays frosted glass effect when scrolling</done>
</task>

<task type="auto">
  <name>Task 2: Code Block Text Color Verification</name>
  <files>src/styles/global.css, src/pages/articles/[slug].astro</files>
  <read_first>src/styles/global.css, src/pages/articles/[slug].astro</read_first>
  <action>
Verify code block text color matches UI-02 spec (#111111 text on #1f1f1f background).

Update to match spec:
- Code block background: `#1f1f1f` (gray)
- Code block text: `#111111` (near-black, for readability)

For global.css:
```
pre {
  background: #1f1f1f !important;
  color: #111111;
}
```

For inline code in [slug].astro:
```
.article-body :global(code) {
  background: #1f1f1f;
  color: #111111;
}
```
  </action>
  <acceptance_criteria>
- Code blocks have gray (#1f1f1f) background
- Code text is black (#111111) for readability
  </acceptance_criteria>
  <verify>
<automated>grep -E "(background.*#1f1f1f|color.*#111111)" src/styles/global.css src/pages/articles/\[slug\].astro && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Code blocks have gray background with black text</done>
</task>

</tasks>

<verification>
- Header shows frosted glass effect on scroll > 20px
- Code blocks in articles have gray (#1f1f1f) background with black (#111111) text
</verification>

<success_criteria>
Header frosted glass effect works on scroll, code blocks have readable gray bg with black text
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-ux-polish-and-content-management/09-01-SUMMARY.md`
</output>