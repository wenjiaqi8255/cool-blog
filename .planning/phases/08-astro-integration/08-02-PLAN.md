---
phase: 08-astro-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/pages/articles/index.astro
  - src/pages/articles/[slug].astro
autonomous: true
requirements:
  - ASTRO-01
  - ASTRO-02
  - ASTRO-03
  - ASTRO-04

must_haves:
  truths:
    - "/articles page fetches from database and renders ArticleCard grid"
    - "/articles/[slug] renders full article with Markdown and syntax highlighting"
    - "404 page displays when article not found or filtered out"
    - "Old Markdown files NOT displayed (database-only mode)"
  artifacts:
    - path: "src/pages/articles/index.astro"
      provides: "Article list page server-rendered from database"
      renders: "ArticleList with database articles"
    - path: "src/pages/articles/[slug].astro"
      provides: "Individual article page with Markdown rendering"
      renders: "Full article body with Shiki syntax highlighting"
  key_links:
    - from: "src/pages/articles/index.astro"
      to: "src/lib/articles.ts"
      via: "await listPublishedArticles()"
      pattern: "listPublishedArticles"
    - from: "src/pages/articles/[slug].astro"
      to: "src/lib/articles.ts"
      via: "await getPublishedArticleBySlug(slug)"
      pattern: "getPublishedArticleBySlug"
---

<objective>
Implement article list and detail pages that fetch from database with proper filtering.

Purpose: Create server-rendered pages that display articles from Neon Postgres with Markdown rendering.
Output: Updated /articles index page and /articles/[slug] detail page
</objective>

<execution_context>
@/Users/wenjiaqi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenjiaqi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/articles.ts (created in Plan 08-01)
@src/pages/articles/index.astro (existing list page to modify)
@src/pages/articles/[slug].astro (existing detail page to modify)
@src/components/articles/ArticleList.astro (list component to adapt)
@.planning/phases/08-astro-integration/08-CONTEXT.md (user decisions)
@.planning/phases/08-astro-integration/08-UI-SPEC.md (UI specs)

**UI Spec requirements:**
- Article list page: grid of cards, max-width 1600px, 40px padding, 6 articles per page
- Article detail: max-width 800px, title 24px weight 300, body 14px line-height 1.5
- 404 page for not found articles
- Database-only mode: no fallback to content collections
</context>

<tasks>

<task type="auto">
<name>Task 1: Update article list page for database</name>
<files>src/pages/articles/index.astro</files>
<read_first>
    - src/pages/articles/index.astro (existing page)
    - src/components/articles/ArticleList.astro (list component)
  </read_first>
  <action>
Update `src/pages/articles/index.astro` to fetch from database instead of content collections:

1. Remove content collection import and usage:
   - Remove: `import { getCollection, render } from 'astro:content';`
   - Remove: `const { getCollection } = await import('astro:content');`
   - Remove: `const allArticles = await getCollection('articles');`

2. Add database import and query:
   - Add: `import { listPublishedArticles } from '../../lib/articles';`
   - Add in frontmatter: `const articles = await listPublishedArticles();`
   - This fetches published, non-deleted articles sorted by date DESC

3. Update article count calculation:
   - Replace: `const articleCount = allArticles.filter(a => !a.data.draft).length;`
   - With: `const articleCount = articles.length;`

4. Keep existing page structure:
   - Header, page title, description remain
   - But replace `<ArticleList />` with inline article grid or modified ArticleList

5. For inline implementation (recommended):
   - Import ArticleCard component
   - Use BentoGrid pattern from existing code
   - Render articles.slice(0, PAGE_SIZE).map to ArticleCard
   - Each ArticleCard receives article object directly from database

PAGE_SIZE = 6 per UI spec.

6. Empty state handling (per UI spec):
   - If articles.length === 0, show "No articles yet" heading
   - Show "Articles will appear here once published." body

Keep existing styling (max-width 1600px, padding 40px, page-title 48px, etc.)
  </action>
  <verify>
    <automated>grep -n "listPublishedArticles" src/pages/articles/index.astro</automated>
  </verify>
  <done>/articles page fetches from database and displays published articles</done>
</task>

<task type="auto">
<name>Task 2: Update article detail page for database</name>
  <files>src/pages/articles/[slug].astro</files>
  <read_first>
    - src/pages/articles/[slug].astro (existing page)
  </read_first>
  <action>
Update `src/pages/articles/[slug].astro` to fetch from database instead of content collections:

1. Replace content collection logic:
   - Remove: `import { getCollection, render } from 'astro:content';`
   - Remove: `export async function getStaticPaths()` and filtering logic
   - This becomes a dynamic SSR page, not static paths

2. Add database imports:
   - Add: `import { getPublishedArticleBySlug } from '../../lib/articles';`
   - Add: `import { db } from '../../db/index';`
   - Add: `import { articles as articlesTable } from '../../db/schema';`
   - Add: `import { eq } from 'drizzle-orm';`

3. Add SSR mode export:
   - Add: `export const prerender = false;` (makes this SSR)

4. Fetch article in frontmatter:
```typescript
const { slug } = Astro.params;
if (!slug) {
  return Astro.redirect('/articles');
}

const article = await getPublishedArticleBySlug(slug);

if (!article) {
  return Astro.redirect('/404');
}
```

5. Update data access:
   - Replace: `const { entry } = Astro.props;`
   - Replace: `const { data, id } = entry;`
   - Replace: `const { title, date, tags, excerpt } = data;`
   - With direct article fields: `const { title, date, tags, excerpt, body } = article;`

6. For Markdown rendering:
   - Import markdown-it or use unified/remark for rendering
   - Use existing Shiki setup for syntax highlighting
   - The body field contains raw Markdown from database

Option A - Use markdown-it (simpler):
```typescript
import MarkdownIt from 'markdown-it';
const md = new MarkdownIt({ html: false, linkify: true, typographer: true });
const renderedContent = md.render(body);
```

Option B - Use unified/remark (more control):
- Follow existing Astro patterns in src/lib/content/parser.ts

Choose based on existing parser patterns. If parser.ts exists and works, use it.

7. Update template to use:
   - `title` instead of `data.title`
   - `date` directly (Date object)
   - `tags` directly (array or null)
   - Rendered content instead of `<Content />`

8. Keep copy button script for code blocks.

9. Keep 404 handling - either redirect or show error page. Per UI spec: "Article not found" heading with body "The article may have been removed or the URL is incorrect."

10. Keep existing styling (max-width 800px, article-title 28px, article-body 16px, etc.)

Date format: Use localized format "March 31, 2026" per UI spec:
```typescript
const formattedDate = new Intl.DateTimeFormat('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric'
}).format(date);
```
  </action>
  <verify>
    <automated>grep -n "getPublishedArticleBySlug\|prerender = false" src/pages/articles/[slug].astro</automated>
  </verify>
  <done>/articles/[slug] fetches from database and renders article with Markdown</done>
</task>

<task type="auto">
<name>Task 3: Create 404 page for article not found</name>
<files>src/pages/404.astro</files>
<read_first>
    - src/layouts/BaseLayout.astro (for layout reference)
  </read_first>
  <action>
Ensure 404 page exists at src/pages/404.astro with proper messaging:

Per UI spec copy:
- Heading: "Article not found"
- Body: "The article may have been removed or the URL is incorrect."

If 404.astro already exists:
- Verify it has appropriate messaging for article not found
- If generic, update to be reusable for both missing pages and articles

If creating new:
- Use BaseLayout
- Center content
- Include link back to /articles
- Match existing styling patterns

This handles the case when:
- Article slug doesn't exist
- Article is draft (filtered out)
- Article is soft-deleted (filtered out)
  </action>
  <verify>
    <automated>ls src/pages/404.astro && grep -n "Article not found\|not found" src/pages/404.astro</automated>
  </verify>
  <done>404 page exists with proper "Article not found" messaging</done>
</task>

</tasks>

<verification>
- [ ] /articles displays grid of published articles from database
- [ ] /articles/[slug] renders full article with syntax highlighting
- [ ] 404 page shows for non-existent or filtered articles
- [ ] Database-only mode: no content collection fallback
- [ ] SSR per request enabled (prerender = false on detail page)
</verification>

<success_criteria>
Article list and detail pages fetch from database with proper filtering. Markdown renders with syntax highlighting. 404 handling works for missing/filtered articles.
</success_criteria>

<output>
After completion, create `.planning/phases/08-astro-integration/08-02-SUMMARY.md`
</output>
