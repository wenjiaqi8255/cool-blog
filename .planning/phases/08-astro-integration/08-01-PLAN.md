---
phase: 08-astro-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/articles.ts
  - src/components/articles/ArticleCard.astro
  - src/components/articles/ArticleList.astro
autonomous: true
requirements:
  - ASTRO-01
  - ASTRO-02
  - ASTRO-05
  - ASTRO-06

must_haves:
  truths:
    - "Article list page fetches from database with status='published' and deleted_at IS NULL"
    - "ArticleCard component renders database article objects (not CollectionEntry)"
    - "Database queries use Drizzle ORM with parameterized queries"
  artifacts:
    - path: "src/lib/articles.ts"
      provides: "Database query functions for articles"
      exports: ["listPublishedArticles", "getPublishedArticleBySlug"]
    - path: "src/components/articles/ArticleCard.astro"
      provides: "Article card component for database articles"
      props: "id, title, slug, date, tags, excerpt, body"
  key_links:
    - from: "src/lib/articles.ts"
      to: "src/db/index.ts"
      via: "drizzle query"
      pattern: "drizzle.*select.*from.*articles"
---

<objective>
Create database query layer and adapt ArticleCard component for database-driven articles.

Purpose: Establish the data layer that fetches articles from Neon Postgres with proper filtering for published status and non-deleted articles.
Output: New article query functions in src/lib/articles.ts, updated ArticleCard component
</objective>

<execution_context>
@/Users/wenjiaqi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenjiaqi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/db/schema.ts (articles table schema)
@src/db/index.ts (database connection)
@src/components/articles/ArticleCard.astro (existing card component to adapt)
@.planning/phases/08-astro-integration/08-CONTEXT.md (user decisions)
@.planning/phases/08-astro-integration/08-UI-SPEC.md (UI specs)

**Database schema from schema.ts:**
- articles table has: id, title, slug, date, tags (array), excerpt, body, status, deleted_at, created_at, updated_at
- Status values: 'draft' | 'published'
- Soft delete: deleted_at IS NULL = visible

**ArticleCard props interface:**
- Current: Uses CollectionEntry<'articles'>
- New: id, title, slug, date, tags, excerpt, body
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create database article query functions</name>
  <files>src/lib/articles.ts</files>
  <read_first>
    - src/db/schema.ts (articles table structure)
    - src/db/index.ts (database connection)
  </read_first>
  <action>
Create new file `src/lib/articles.ts` with two exported functions:

1. `listPublishedArticles()` - Returns all published, non-deleted articles sorted by date DESC
   - Query: `SELECT * FROM articles WHERE status = 'published' AND deleted_at IS NULL ORDER BY date DESC`
   - Use Drizzle ORM: `await db.select().from(articles).where(...)`
   - Return type: `Article[]` (define Article interface matching db schema)

2. `getPublishedArticleBySlug(slug: string)` - Returns single article by slug
   - Query: `SELECT * FROM articles WHERE slug = $1 AND status = 'published' AND deleted_at IS NULL`
   - Use parameterized query with Drizzle: `eq(articles.slug, slug)`
   - Return: `Article | undefined`

Define Article interface matching schema.ts fields:
```typescript
interface Article {
  id: number;
  title: string;
  slug: string;
  date: Date;
  tags: string[] | null;
  excerpt: string | null;
  body: string;
  status: 'draft' | 'published';
  deleted_at: Date | null;
  created_at: Date;
  updated_at: Date;
}
```

Import from drizzle-orm: `eq, and, isNull, desc` for building queries.
  </action>
  <verify>
    <automated>grep -n "export.*listPublishedArticles\|export.*getPublishedArticleBySlug" src/lib/articles.ts</automated>
  </verify>
  <done>src/lib/articles.ts exports listPublishedArticles and getPublishedArticleBySlug functions</done>
</task>

<task type="auto">
  <name>Task 2: Adapt ArticleCard for database articles</name>
  <files>src/components/articles/ArticleCard.astro</files>
  <read_first>
    - src/components/articles/ArticleCard.astro (existing component to modify)
  </read_first>
  <action>
Modify `src/components/articles/ArticleCard.astro` to accept database article objects instead of CollectionEntry:

1. Replace the Props interface:
```typescript
interface Props {
  article: {
    id: number;
    title: string;
    slug: string;
    date: Date;
    tags: string[] | null;
    excerpt: string | null;
    body: string;
  };
}
```

2. Update component logic:
   - Remove `CollectionEntry` import
   - Change `const { data, id } = article` to `const { id, title: articleTitle, slug, date, tags, excerpt, body } = article`
   - Use `title` instead of `data.title`, etc.
   - Remove `const slug = id.replace(/\.md$/, '')` - slug is now direct field
   - For reading time calculation: use `body.split(/\s+/).length` (body is already string, not in entry.body)

3. Keep existing styling and markup unchanged - only update data access patterns.

4. Update date formatting: The database returns Date objects, so use:
```typescript
const formattedDate = date.toISOString().split('T')[0];
```
(Already correct in original, verify it works with Date object from DB)
  </action>
  <verify>
    <automated>grep -n "article: {" src/components/articles/ArticleCard.astro && grep -n "slug: string" src/components/articles/ArticleCard.astro</automated>
  </verify>
  <done>ArticleCard accepts database article object with id, title, slug, date, tags, excerpt, body fields</done>
</task>

</tasks>

<verification>
- [ ] listPublishedArticles() returns articles with status='published' and deleted_at IS NULL
- [ ] getPublishedArticleBySlug() returns article matching slug with published status
- [ ] ArticleCard renders database article correctly with title, date, tags, excerpt
- [ ] Reading time calculated from body word count
</verification>

<success_criteria>
Database queries fetch only published, non-deleted articles. ArticleCard component works with database article objects.
</success_criteria>

<output>
After completion, create `.planning/phases/08-astro-integration/08-01-SUMMARY.md`
</output>
