---
phase: 04-seo-launch
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/pages/rss.xml.js
  - astro.config.mjs
autonomous: true
requirements:
  - SEO-02

must_haves:
  truths:
    - "User can subscribe to an RSS feed of articles"
    - "RSS feed includes full article content"
    - "RSS feed includes article images as media:content"
  artifacts:
    - path: "src/pages/rss.xml.js"
      provides: "RSS feed endpoint with full content and images"
      exports: "GET function returning RSS XML"
    - path: "astro.config.mjs"
      provides: "Site URL configuration for RSS generation"
      contains: "site: 'https://kernel-panic.dev'"
  key_links:
    - from: "src/pages/rss.xml.js"
      to: "src/content/config.ts"
      via: "getCollection('articles')"
    - from: "src/pages/rss.xml.js"
      to: "BaseLayout.astro"
      via: "RSS auto-discovery link already added in 04-01"
---

<objective>
Generate RSS feed for article subscriptions with full content and images.

Purpose: Allow users to subscribe to articles via RSS readers.
Output: rss.xml.js endpoint at /rss.xml
</objective>

<execution_context>
@/Users/wenjiaqi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenjiaqi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/content/config.ts
@astro.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify site URL in astro.config.mjs</name>
  <files>astro.config.mjs</files>
  <read_first>
    - astro.config.mjs (current configuration)
  </read_first>
  <action>
    Check if astro.config.mjs already has site configured. If not, add:
    ```javascript
    export default defineConfig({
      site: 'https://kernel-panic.dev',
      // ... existing config
    });
    ```
  </action>
  <acceptance_criteria>
    - [ ] astro.config.mjs contains site property with URL
    - [ ] site URL is 'https://kernel-panic.dev'
  </acceptance_criteria>
  <verify>
    grep "site:" astro.config.mjs
  </verify>
  <done>astro.config.mjs has site URL configured for RSS generation</done>
</task>

<task type="auto">
  <name>Task 2: Create RSS feed endpoint at src/pages/rss.xml.js</name>
  <files>src/pages/rss.xml.js</files>
  <read_first>
    - src/content/config.ts (to understand article collection schema)
    - src/pages/articles/[slug].astro (to understand article structure)
  </read_first>
  <action>
    Create src/pages/rss.xml.js with the following:

    1. Import rss from '@astrojs/rss'
    2. Import getCollection from 'astro:content'

    3. Create GET function that:
       - Fetches all non-draft articles: `await getCollection('articles', ({ data }) => !data.draft)`
       - Maps articles to RSS items with:
         - title: article.data.title
         - pubDate: article.data.date
         - description: article.data.excerpt
         - link: `/articles/${article.id}/`
         - content: article.body (full markdown content)
         - customData: media:content element with article image (if present)

    4. Return rss({
       title: 'KERNEL_PANIC / ARCHITECTURE & SYSTEMS',
       description: 'Computing as craft — Technical writing and portfolio',
       site: context.site,
       items: mapped items,
       customData: '<language>en-us</language>'
    })

    5. Use content for full article body (markdown) per user decision
  </action>
  <acceptance_criteria>
    - [ ] rss.xml.js exists at src/pages/rss.xml.js
    - [ ] rss.xml.js imports from '@astrojs/rss'
    - [ ] rss.xml.js uses getCollection('articles')
    - [ ] RSS feed includes content field with article.body
    - [ ] RSS feed includes media:content for article images
  </acceptance_criteria>
  <verify>
    ls src/pages/rss.xml.js && \
    grep "import.*rss" src/pages/rss.xml.js && \
    grep "article.body" src/pages/rss.xml.js && \
    grep "media:content" src/pages/rss.xml.js
  </verify>
  <done>RSS feed endpoint created with full content and images</done>
</task>

</tasks>

<verification>
- RSS feed is accessible at /rss.xml
- RSS feed validates against RSS 2.0 spec
- Full article content included in feed
- Article images included as media:content
</verification>

<success_criteria>
User can subscribe to an RSS feed of articles with full content.
</success_criteria>

<output>
After completion, create `.planning/phases/04-seo-launch/04-02-SUMMARY.md`
</output>