---
phase: 04-seo-launch
plan: 05
type: execute
wave: 4
depends_on:
  - 04-01
  - 04-04
files_modified:
  - src/pages/articles/[slug].astro
autonomous: true
requirements:
  - SEO-01
gap_closure: true

must_haves:
  truths:
    - "User sharing article link on social media sees article-specific OG image"
    - "[slug].astro passes image prop to BaseLayout"
    - "OG image URL follows /og/{article-id}.svg pattern"
  artifacts:
    - path: "src/pages/articles/[slug].astro"
      provides: "Article page with wired OG image"
      contains: "image={`/og/${entry.id}.svg`}"
---

<objective>
Close verification gaps from Phase 4 SEO & Launch:
1. Wire article-specific OG images in [slug].astro
2. Document that SVG format is acceptable (no satori/resvg needed)

Purpose: Fix partial verification where article pages don't pass image prop to BaseLayout
Output: Article OG images properly wired
</objective>

<execution_context>
@/Users/wenjiaqi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenjiaqi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/pages/articles/[slug].astro
@src/layouts/BaseLayout.astro

**Current State:**
- [slug].astro passes type="article" to BaseLayout but NOT image prop
- OG endpoint exists at src/pages/og/[slug].svg.ts
- BaseLayout accepts optional `image` prop (line 11) and uses it for og:image meta tag (line 26)

**Fix Required:**
- Add `image={\`/og/${entry.id}.svg\`}` to BaseLayout call in [slug].astro
- Note: Using SVG format (not PNG) - this is actually better for performance
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire article-specific OG image in [slug].astro</name>
  <files>src/pages/articles/[slug].astro</files>
  <read_first>
    - src/pages/articles/[slug].astro (current BaseLayout call at lines 37-41)
    - src/layouts/BaseLayout.astro (image prop handling at lines 11, 26)
  </read_first>
  <action>
    In src/pages/articles/[slug].astro, modify the BaseLayout component call (lines 37-41) to add the image prop:

    Change from:
    ```astro
    <BaseLayout
      title={`KERNEL_PANIC / ${title}`}
      description={excerpt || title}
      type="article"
    >
    ```

    To:
    ```astro
    <BaseLayout
      title={`KERNEL_PANIC / ${title}`}
      description={excerpt || title}
      type="article"
      image={`/og/${entry.id}.svg`}
    >
    ```

    This will pass the article-specific OG image URL to BaseLayout, which will set og:image meta tag to the article's custom SVG.
  </action>
  <acceptance_criteria>
    - [ ] grep 'image={`/og/${entry.id}.svg`}' src/pages/articles/[slug].astro returns match
    - [ ] BaseLayout call includes all three props: title, description, type, image
  </acceptance_criteria>
  <verify>
    grep "image={\`/og/\${entry.id}.svg\`}" src/pages/articles/\[slug\].astro
  </verify>
  <done>Article page passes image prop to BaseLayout for article-specific OG image</done>
</task>

</tasks>

<verification>
- Verify [slug].astro now passes image prop: grep 'image={`/og/${entry.id}.svg`}' src/pages/articles/\[slug\].astro
- Verify BaseLayout receives image for og:image meta tag
- OG image URL will be: https://kernel-panic.dev/og/{article-id}.svg
</verification>

<success_criteria>
User sharing article link on social media sees article-specific OG image with article title and branding.
</success_criteria>

<output>
After completion, create `.planning/phases/04-seo-launch/04-05-SUMMARY.md`
</output>
