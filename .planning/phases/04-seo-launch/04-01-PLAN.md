---
phase: 04-seo-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/BaseLayout.astro
  - src/pages/index.astro
  - src/pages/articles/index.astro
  - src/pages/articles/[slug].astro
autonomous: true
requirements:
  - SEO-01

must_haves:
  truths:
    - "User sharing a link on social media sees proper Open Graph previews"
    - "User sharing a link on Twitter sees proper Twitter Card previews"
    - "Page titles follow 'KERNEL_PANIC / ... | Page Title' format"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "Base layout with OG and Twitter meta tags"
      contains: "og:title, og:description, og:image, og:type, og:url"
    - path: "src/layouts/BaseLayout.astro"
      provides: "Twitter Card meta tags"
      contains: "twitter:card, twitter:title, twitter:description, twitter:image"
    - path: "src/pages/articles/[slug].astro"
      provides: "Article-specific OG images via props"
      contains: "ogImage, type: article"
  key_links:
    - from: "src/pages/index.astro"
      to: "BaseLayout.astro"
      via: "passes title, description, image props"
    - from: "src/pages/articles/[slug].astro"
      to: "BaseLayout.astro"
      via: "passes article-specific title, description, image"
---

<objective>
Add Open Graph and Twitter Card meta tags to all pages with article-specific images.

Purpose: Enable social platforms to generate rich preview cards when links are shared.
Output: Extended BaseLayout.astro with OG/twitter meta, updated page templates.
</objective>

<execution_context>
@/Users/wenjiaqi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenjiaqi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/layouts/BaseLayout.astro
@src/pages/index.astro
@src/pages/articles/index.astro
@src/pages/articles/[slug].astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend BaseLayout.astro with OG and Twitter meta tags</name>
  <files>src/layouts/BaseLayout.astro</files>
  <read_first>
    - src/layouts/BaseLayout.astro (current state with title, description props)
  </read_first>
  <action>
    Update BaseLayout.astro interface and implementation:

    1. Add to Props interface:
       - image?: string (optional og:image, defaults to /og-default.png)
       - type?: 'website' | 'article' (og:type, defaults to website)
       - articleDate?: Date (optional for article pages)

    2. Add constants:
       - siteUrl = 'https://kernel-panic.dev' (or from import.meta.env.SITE)
       - Use existing title and description as fallbacks

    3. Add in <head> section BEFORE </head>:
       ```html
       <!-- Open Graph -->
       <meta property="og:title" content={title} />
       <meta property="og:description" content={description} />
       <meta property="og:image" content={image ? `${siteUrl}${image}` : `${siteUrl}/og-default.png`} />
       <meta property="og:type" content={type} />
       <meta property="og:url" content={Astro.url.href} />

       <!-- Twitter Card -->
       <meta name="twitter:card" content="summary_large_image" />
       <meta name="twitter:title" content={title} />
       <meta name="twitter:description" content={description} />
       <meta name="twitter:image" content={image ? `${siteUrl}${image}` : `${siteUrl}/og-default.png`} />

       <!-- RSS auto-discovery -->
       <link rel="alternate" type="application/rss+xml" title="RSS Feed" href="/rss.xml" />

       <!-- Canonical URL -->
       <link rel="canonical" href={Astro.url.href} />
       ```

    4. Keep existing: charset, viewport, description, favicon, title
  </action>
  <acceptance_criteria>
    - [ ] BaseLayout.astro contains "og:title" in meta property
    - [ ] BaseLayout.astro contains "twitter:card" with value "summary_large_image"
    - [ ] BaseLayout.astro contains "og:type" property
    - [ ] BaseLayout.astro contains canonical link tag
    - [ ] BaseLayout.astro contains RSS alternate link
  </acceptance_criteria>
  <verify>
    grep -n "og:title" src/layouts/BaseLayout.astro && \
    grep -n "twitter:card" src/layouts/BaseLayout.astro && \
    grep -n "canonical" src/layouts/BaseLayout.astro
  </verify>
  <done>BaseLayout.astro has all required OG and Twitter meta tags with proper fallbacks</done>
</task>

<task type="auto">
  <name>Task 2: Update index.astro to pass OG props</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (current state)
    - src/layouts/BaseLayout.astro (updated in Task 1)
  </read_first>
  <action>
    Update src/pages/index.astro:

    1. Update BaseLayout import to include props:
       Pass title in format "KERNEL_PANIC / ARCHITECTURE & SYSTEMS | Home"

    2. Pass props to BaseLayout:
       ```astro
       <BaseLayout
         title="KERNEL_PANIC / ARCHITECTURE & SYSTEMS | Home"
         description="Computing as craft — Technical writing and portfolio"
         type="website"
       />
       ```

    3. Keep existing content (BentoGrid, hero section)
  </action>
  <acceptance_criteria>
    - [ ] index.astro passes title with " | Home" suffix
    - [ ] index.astro passes description prop
    - [ ] index.astro passes type="website"
  </acceptance_criteria>
  <verify>
    grep -E "title=.*\|" src/pages/index.astro && \
    grep "type=\"website\"" src/pages/index.astro
  </verify>
  <done>Homepage passes correct OG props to BaseLayout</done>
</task>

<task type="auto">
  <name>Task 3: Update articles/index.astro for articles list page OG</name>
  <files>src/pages/articles/index.astro</files>
  <read_first>
    - src/pages/articles/index.astro (current state)
  </read_first>
  <action>
    Update src/pages/articles/index.astro:

    1. Update BaseLayout props:
       ```astro
       <BaseLayout
         title="KERNEL_PANIC / ARTICLES | Articles"
         description="Technical writing — Deep dives into architecture, systems, and computing"
         type="website"
       />
       ```
  </action>
  <acceptance_criteria>
    - [ ] articles/index.astro passes title with " | Articles" suffix
    - [ ] articles/index.astro passes description for articles list
  </acceptance_criteria>
  <verify>
    grep -E "title=.*ARTICLES.*\|" src/pages/articles/index.astro
  </verify>
  <done>Articles list page has correct OG meta</done>
</task>

<task type="auto">
  <name>Task 4: Update [slug].astro for article-specific OG images</name>
  <files>src/pages/articles/[slug].astro</files>
  <read_first>
    - src/pages/articles/[slug].astro (current state)
  </read_first>
  <action>
    Update src/pages/articles/[slug].astro:

    1. Extract article image from frontmatter (article.data.image) or first image in body
    2. Update BaseLayout props:
       ```astro
       <BaseLayout
         title={`KERNEL_PANIC / ${article.data.title}`}
         description={article.data.excerpt}
         image={article.data.image ? `/og/${article.id}.png` : undefined}
         type="article"
       />
       ```

    3. Note: Dynamic OG image generation will be handled in a dedicated task (covered in later plan for satori implementation)
  </action>
  <acceptance_criteria>
    - [ ] [slug].astro passes type="article" to BaseLayout
    - [ ] [slug].astro includes article title in page title
    - [ ] [slug].astro passes article excerpt as description
  </acceptance_criteria>
  <verify>
    grep "type=\"article\"" src/pages/articles/\[slug\].astro && \
    grep "article.data.title" src/pages/articles/\[slug\].astro
  </verify>
  <done>Article pages have type="article" OG meta with dynamic titles</done>
</task>

</tasks>

<verification>
- All pages have proper OG meta tags with correct title format
- Twitter Card uses summary_large_image
- Article pages have type="article"
- RSS auto-discovery link present
</verification>

<success_criteria>
User sharing a link on social media sees proper Open Graph and Twitter Card previews with branded images.
</success_criteria>

<output>
After completion, create `.planning/phases/04-seo-launch/04-01-SUMMARY.md`
</output>