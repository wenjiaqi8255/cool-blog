---
phase: 04-seo-launch
plan: 04
type: execute
wave: 3
depends_on:
  - 04-01
files_modified:
  - src/pages/og/[slug].png.ts
  - src/pages/og/default.png.ts
autonomous: true
requirements:
  - SEO-01

must_haves:
  truths:
    - "Each article has its own generated OG image with title and branding"
    - "Default OG image available for pages without article-specific images"
    - "OG images match the terminal/brutalist aesthetic (dark background, monospace font)"
  artifacts:
    - path: "src/pages/og/[slug].png.ts"
      provides: "Dynamic OG image generation for each article"
      exports: "GET function returning PNG image"
    - path: "src/pages/og/default.png.ts"
      provides: "Default OG image for homepage and list pages"
      exports: "GET function returning PNG image"
  key_links:
    - from: "src/pages/og/[slug].png.ts"
      to: "BaseLayout.astro"
      via: "Referenced as /og/${article.id}.png in og:image prop"
    - from: "src/pages/articles/[slug].astro"
      to: "src/pages/og/[slug].png.ts"
      via: "Passes /og/${article.id}.png as image prop"
---

<objective>
Implement dynamic OG image generation using Satori for article-specific branded cards.

Purpose: Each article gets a unique OG image with terminal/brutalist aesthetic matching the site brand.
Output: Dynamic OG image endpoints for articles and default
</objective>

<execution_context>
@/Users/wenjiaqi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenjiaqi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/pages/articles/[slug].astro
@src/layouts/BaseLayout.astro

**Note:** Will need to install satori and @resvg/resvg-js packages. Research recommends:
- satori: 0.26.0 for JSX-to-SVG generation
- @resvg/resvg-js for SVG-to-PNG conversion
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install satori and resvg dependencies</name>
  <files>package.json</files>
  <read_first>
    - package.json (current dependencies)
  </read_first>
  <action>
    Install the following packages:
    ```bash
    npm install satori @resvg/resvg-js
    ```

    Add to package.json dependencies:
    - "satori": "^0.26.0"
    - "@resvg/resvg-js": "^2.6.0"

    Note: These are needed for JSX-to-image generation in the build/edge environment.
  </action>
  <acceptance_criteria>
    - [ ] package.json contains satori dependency
    - [ ] package.json contains @resvg/resvg-js dependency
  </acceptance_criteria>
  <verify>
    grep "satori" package.json && grep "resvg" package.json
  </verify>
  <done>Satori and resvg packages installed</done>
</task>

<task type="auto">
  <name>Task 2: Create default OG image generator</name>
  <files>src/pages/og/default.png.ts</files>
  <read_first>
    - src/pages/og/default.png.ts (does not exist yet)
  </read_first>
  <action>
    Create src/pages/og/default.png.ts:

    1. Create GET function that generates a branded OG image
    2. Use Satori to create SVG with:
       - Background: #111 (dark, matching terminal aesthetic)
       - Brand text: "KERNEL_PANIC / ARCHITECTURE & SYSTEMS" in JetBrains Mono
       - Tagline: "Computing as craft" in Inter
       - Layout: Centered vertically and horizontally

    3. Convert SVG to PNG using @resvg/resvg-js
    4. Return PNG with proper Content-Type header

    5. Image dimensions: 1200x630 (standard OG size)
  </action>
  <acceptance_criteria>
    - [ ] src/pages/og/default.png.ts exists
    - [ ] Uses satori to generate SVG
    - [ ] Uses @resvg/resvg-js to convert to PNG
    - [ ] Returns Content-Type: image/png
    - [ ] Image has dark background (#111) and brand text
  </acceptance_criteria>
  <verify>
    ls src/pages/og/default.png.ts && \
    grep "satori" src/pages/og/default.png.ts && \
    grep "Resvg" src/pages/og/default.png.ts
  </verify>
  <done>Default OG image generator created</done>
</task>

<task type="auto">
  <name>Task 3: Create dynamic article OG image generator</name>
  <files>src/pages/og/[slug].png.ts</files>
  <read_first>
    - src/pages/og/[slug].png.ts (does not exist yet)
    - src/content/config.ts (to understand article collection)
  </read_first>
  <action>
    Create src/pages/og/[slug].png.ts:

    1. Create getStaticPaths that fetches all non-draft articles
    2. For each article, return params: { slug: article.id } and props: { article }

    3. Create GET function that:
       - Uses Satori to generate SVG with:
         - Background: #111 (dark)
         - Brand: "KERNEL_PANIC / ARCHITECTURE & SYSTEMS" at top
         - Article title: Large, centered below brand
         - Date: Smaller, at bottom (format: YYYY-MM-DD)
         - Font: JetBrains Mono for all text
       - Dimensions: 1200x630

    4. Convert SVG to PNG using @resvg/resvg-js
    5. Return PNG with proper Content-Type header
  </action>
  <acceptance_criteria>
    - [ ] src/pages/og/[slug].png.ts exists
    - [ ] Has getStaticPaths returning all article slugs
    - [ ] Uses satori to generate SVG with article title
    - [ ] Uses @resvg/resvg-js to convert to PNG
    - [ ] Returns Content-Type: image/png
  </acceptance_criteria>
  <verify>
    ls src/pages/og/\[slug\].png.ts && \
    grep "getStaticPaths" src/pages/og/\[slug\].png.ts && \
    grep "satori" src/pages/og/\[slug\].png.ts
  </verify>
  <done>Dynamic article OG image generator created</done>
</task>

</tasks>

<verification>
- /og/default.png accessible and returns valid PNG
- /og/[slug].png accessible for each article and returns valid PNG
- OG images match terminal/brutalist aesthetic with dark background
</verification>

<success_criteria>
User sharing article links sees branded OG images with article title and site identity.
</success_criteria>

<output>
After completion, create `.planning/phases/04-seo-launch/04-04-SUMMARY.md`
</output>