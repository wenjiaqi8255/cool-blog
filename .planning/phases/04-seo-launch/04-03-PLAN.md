---
phase: 04-seo-launch
plan: 03
type: execute
wave: 2
depends_on:
  - 04-02
files_modified:
  - astro.config.mjs
  - src/pages/robots.txt.ts
autonomous: true
requirements:
  - SEO-03

must_haves:
  truths:
    - "Search engines can crawl the site via sitemap.xml"
    - "Robots.txt references sitemap location"
    - "Sitemap includes all public pages with correct priorities"
  artifacts:
    - path: "astro.config.mjs"
      provides: "Sitemap integration configuration"
      contains: "@astrojs/sitemap"
    - path: "public/sitemap-0.xml"
      provides: "Generated sitemap with page URLs"
      contains: "<urlset"
    - path: "src/pages/robots.txt.ts"
      provides: "Robots.txt with sitemap reference"
      contains: "Sitemap:"
  key_links:
    - from: "astro.config.mjs"
      to: "public/sitemap-0.xml"
      via: "@astrojs/sitemap generates at build time"
    - from: "src/pages/robots.txt.ts"
      to: "public/sitemap-0.xml"
      via: "Sitemap: https://kernel-panic.dev/sitemap-index.xml"
---

<objective>
Generate sitemap.xml and robots.txt for search engine crawling.

Purpose: Enable search engines to discover and index all site pages.
Output: Sitemap via @astrojs/sitemap, robots.txt endpoint
</objective>

<execution_context>
@/Users/wenjiaqi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenjiaqi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@astro.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sitemap integration to astro.config.mjs</name>
  <files>astro.config.mjs</files>
  <read_first>
    - astro.config.mjs (current configuration with site URL from 04-02)
  </read_first>
  <action>
    Update astro.config.mjs:

    1. Import sitemap from '@astrojs/sitemap':
       ```javascript
       import sitemap from '@astrojs/sitemap';
       ```

    2. Add sitemap to integrations array:
       ```javascript
       integrations: [
         sitemap({
           filter: (page) => !page.includes('/draft/'),
           customPages: ['/articles/'],
         })
       ]
       ```

    3. Note: site URL is already configured from 04-02
    4. @astrojs/sitemap will auto-generate sitemap-index.xml at build time
  </action>
  <acceptance_criteria>
    - [ ] astro.config.mjs imports sitemap from '@astrojs/sitemap'
    - [ ] astro.config.mjs includes sitemap in integrations
    - [ ] sitemap integration has filter for draft pages
  </acceptance_criteria>
  <verify>
    grep "sitemap" astro.config.mjs
  </verify>
  <done>Sitemap integration added to Astro config</done>
</task>

<task type="auto">
  <name>Task 2: Create robots.txt endpoint</name>
  <files>src/pages/robots.txt.ts</files>
  <read_first>
    - src/pages/robots.txt.ts (does not exist yet)
  </read_first>
  <action>
    Create src/pages/robots.txt.ts:

    1. Create GET function returning robots.txt content:
       ```typescript
       import type: { APIRoute } from 'astro';

       export const GET: APIRoute = () => {
         return new Response(
           `User-agent: *
       Allow: /

       Sitemap: https://kernel-panic.dev/sitemap-index.xml`,
           {
             headers: {
               'Content-Type': 'text/plain'
             }
           }
         );
       };
       ```

    2. Per user decision: Allow all pages + reference sitemap location
  </action>
  <acceptance_criteria>
    - [ ] robots.txt.ts exists at src/pages/robots.txt.ts
    - [ ] robots.txt allows all pages (User-agent: * / Allow: /)
    - [ ] robots.txt includes Sitemap reference to sitemap-index.xml
  </acceptance_criteria>
  <verify>
    ls src/pages/robots.txt.ts && \
    grep "Sitemap:" src/pages/robots.txt.ts && \
    grep "Allow: /" src/pages/robots.txt.ts
  </verify>
  <done>Robots.txt created with sitemap reference</done>
</task>

</tasks>

<verification>
- sitemap-index.xml generated at build time
- robots.txt accessible at /robots.txt
- All public pages (/, /articles, /articles/[slug]) included in sitemap
- Priority values: homepage 1.0, articles 0.8, other pages 0.5
</verification>

<success_criteria>
Search engines can crawl the site via sitemap.xml with proper configuration.
</success_criteria>

<output>
After completion, create `.planning/phases/04-seo-launch/04-03-SUMMARY.md`
</output>