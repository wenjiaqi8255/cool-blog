---
phase: 7
plan: 2
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/lib/content/workflow.ts
  - src/lib/mcp/db.ts
  - src/lib/mcp/server.ts
autonomous: true
requirements:
  - WORK-03
  - WORK-04
  - WORK-06
must_haves:
  truths:
    - "User sees preview of rendered article before publishing"
    - "User confirms or rejects article before database write"
    - "User can publish draft article (change status from draft to published)"
  artifacts:
    - path: "src/lib/content/workflow.ts"
      provides: "Workflow orchestration with preview generation"
      exports: ["processMarkdown", "generatePreview"]
    - path: "src/lib/mcp/db.ts"
      provides: "Database update function for publishing drafts"
      exports: ["updateArticleStatus"]
    - path: "src/lib/mcp/server.ts"
      provides: "MCP tool for updating draft status"
      exports: ["update_article tool"]
  key_links:
    - from: "src/lib/content/workflow.ts"
      to: "src/lib/mcp/db.ts"
      via: "publishArticle function calls createArticle"
      pattern: "await createArticle"
---

<objective>
Implement preview display and confirm/reject workflow.

Purpose: Show user the full article preview, get explicit confirmation, then write to database. Also handle draft publishing.

Output: Workflow orchestration with preview generation and publish confirmation.
</objective>

<execution_context>
@/Users/wenjiaqi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenjiaqi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-content-workflow/07-CONTEXT.md
@.planning/phases/07-content-workflow/07-RESEARCH.md

# Phase 6 existing code
@src/lib/mcp/db.ts
@src/lib/mcp/server.ts
@src/lib/mcp/slugify.ts
</context>

<interfaces>
From src/lib/mcp/db.ts:
```typescript
interface CreateArticleInput {
  title: string;
  body: string;
  date?: string;
  tags?: string[];
  excerpt?: string;
  status?: string;
}
export async function createArticle(data: CreateArticleInput): Promise<Article>
```

Expected new export:
```typescript
export async function updateArticleStatus(slug: string, status: 'draft' | 'published'): Promise<Article | null>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add updateArticleStatus to database layer</name>
  <files>src/lib/mcp/db.ts</files>
  <read_first>
    - src/lib/mcp/db.ts
  </read_first>
  <action>
    Add to src/lib/mcp/db.ts:
    1. Export function updateArticleStatus(slug: string, status: 'draft' | 'published'): Promise<Article | null>
    2. Uses Drizzle ORM to update article status by slug
    3. Returns updated article or null if not found
    4. Validates article exists and is not soft-deleted before update
  </action>
  <verify>
    <automated>npx vitest run src/lib/mcp/db.test.ts</automated>
  </verify>
  <done>
    - updateArticleStatus function exported
    - Updates article status in database
    - Returns updated article
  </done>
</task>

<task type="auto">
  <name>Task 2: Add update_article MCP tool</name>
  <files>src/lib/mcp/server.ts</files>
  <read_first>
    - src/lib/mcp/server.ts
  </read_first>
  <action>
    Add to src/lib/mcp/server.ts:
    1. Import updateArticleStatus from './db'
    2. Add MCP tool update_article that:
       - Takes parameters: slug (required), status (optional), title (optional), body (optional)
       - Uses Zod schema for input validation
       - Calls updateArticleStatus to update
       - Returns success: true/false with article or error
    3. Register tool in MCP server tools list
  </action>
  <verify>
    <automated>npx vitest run src/lib/mcp/server.test.ts</automated>
  </verify>
  <done>
    - update_article MCP tool registered
    - Tool validates input with Zod
    - Tool updates article in database
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend workflow for preview and publish</name>
  <files>src/lib/content/workflow.ts</files>
  <read_first>
    - src/lib/content/workflow.ts
    - src/lib/mcp/db.ts
  </read_first>
  <action>
    Extend src/lib/content/workflow.ts:
    1. Export generatePreview(meta, body, slug) that returns formatted preview text for Claude chat
    2. Export async function publishArticle(meta, body, status) that:
       - Calls createArticle from src/lib/mcp/db.ts
       - Returns { success: true, article } or { success: false, error }
    3. Export async function publishDraft(slug) that:
       - Calls updateArticleStatus(slug, 'published')
       - Returns { success: true, article } or { success: false, error }
    4. Preview format includes: Title, Date, Tags, Excerpt, Slug, Body preview (full content)
  </action>
  <verify>
    <automated>npx vitest run src/lib/content/workflow.test.ts</automated>
  </verify>
  <done>
    - generatePreview produces formatted output
    - publishArticle creates new article
    - publishDraft updates draft to published
  </done>
</task>

</tasks>

<verification>
- User sees full content preview in Claude chat
- User confirms or rejects before write
- Draft articles can be published via update_article tool
- Database operations work correctly
</verification>

<success_criteria>
- Preview shows all metadata + full body content
- User can confirm ("publish") to write to database
- User can reject and optionally save as draft
- Drafts can be published later (WORK-06)
</success_criteria>

<output>
After completion, create `.planning/phases/07-content-workflow/07-02-SUMMARY.md`
</output>
