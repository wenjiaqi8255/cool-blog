---
phase: 7
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/content/parser.ts
  - src/lib/content/validator.ts
  - src/lib/mcp/slugify.ts
autonomous: true
requirements:
  - WORK-01
  - WORK-02
  - WORK-07
  - ERR-01
  - ERR-02
  - ERR-05
must_haves:
  truths:
    - "User can submit raw Markdown with YAML frontmatter"
    - "Claude extracts title, date, tags, and excerpt from frontmatter"
    - "Claude generates URL-safe slug from title"
    - "System validates required fields before any database write"
    - "System returns clear errors when title extraction fails"
    - "System validates Markdown format before processing"
  artifacts:
    - path: "src/lib/content/parser.ts"
      provides: "Markdown + frontmatter parsing with gray-matter"
      exports: ["parseMarkdown", "ParsedArticle"]
    - path: "src/lib/content/validator.ts"
      provides: "Zod validation schemas for article content"
      exports: ["articleSchema", "frontmatterSchema", "validateArticle"]
    - path: "src/lib/mcp/slugify.ts"
      provides: "URL-safe slug generation (existing)"
      exports: ["generateSlug"]
  key_links:
    - from: "src/lib/content/parser.ts"
      to: "src/lib/content/validator.ts"
      via: "Zod validation after parsing"
      pattern: "frontmatterSchema.safeParse"
    - from: "src/lib/content/validator.ts"
      to: "src/lib/mcp/slugify.ts"
      via: "slug generation after validation"
      pattern: "generateSlug.*title"
---

<objective>
Implement metadata extraction and slug generation for the content workflow.

Purpose: Parse user-submitted Markdown with frontmatter, extract metadata, validate, and generate URL-safe slugs before any database operations.

Output: Parser and validator modules in src/lib/content/
</objective>

<execution_context>
@/Users/wenjiaqi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenjiaqi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-content-workflow/07-CONTEXT.md
@.planning/phases/07-content-workflow/07-RESEARCH.md

# Phase 6 existing code (reuse these)
@src/lib/mcp/slugify.ts
@src/lib/mcp/db.ts

# Schema reference
@src/db/schema.ts
</context>

<interfaces>
<!-- Key types the executor needs -->
From src/lib/mcp/slugify.ts:
```typescript
export function generateSlug(title: string): string
// Converts title to URL-safe slug, handles Unicode, collisions
```

From src/db/schema.ts (existing):
```typescript
// Articles table with: id, title, slug, date, tags, excerpt, body, status, deleted_at, created_at, updated_at
```

Expected from parser.ts:
```typescript
interface ParsedArticle {
  meta: {
    title: string;
    date?: string;
    tags?: string[];
    excerpt?: string;
  };
  body: string;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create content parser with gray-matter</name>
  <files>src/lib/content/parser.ts</files>
  <read_first>
    - src/lib/mcp/slugify.ts (to understand slug generation pattern)
    - src/db/schema.ts (to understand article structure)
  </read_first>
  <action>
    Install gray-matter dependency:
    ```bash
    npm install gray-matter
    ```

    Create src/lib/content/parser.ts that:
    1. Imports gray-matter as default export
    2. Exports interface ParsedArticle with meta (title, date, tags, excerpt) and body
    3. Exports function parseMarkdown(rawMarkdown: string) returning:
       - success: boolean
       - data?: { meta: {...}, body: string }
       - error?: string
    4. Handles missing frontmatter: returns error "Missing or invalid frontmatter: title is required"
    5. Uses gray-matter to parse YAML frontmatter block
  </action>
  <behavior>
    - parseMarkdown with valid frontmatter returns success: true with meta and body
    - parseMarkdown with missing title returns success: false with error message
    - parseMarkdown with valid YAML but empty title returns error
    - parseMarkdown with no frontmatter block returns error
  </behavior>
  <verify>
    <automated>npx vitest run src/lib/content/parser.test.ts</automated>
  </verify>
  <done>
    - gray-matter installed and working
    - parseMarkdown function exports and parses correctly
    - Missing frontmatter returns clear error message
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Zod validation schemas</name>
  <files>src/lib/content/validator.ts</files>
  <read_first>
    - src/lib/content/parser.ts (from Task 1)
  </read_first>
  <action>
    Create src/lib/content/validator.ts that:
    1. Imports z from 'zod'
    2. Exports frontmatterSchema:
       - title: z.string().min(1, 'Title is required')
       - date: z.string().datetime().optional() OR z.string().regex(/^\d{4}-\d{2}-\d{2}$/)
       - tags: z.array(z.string()).optional()
       - excerpt: z.string().optional()
    3. Exports articleSchema (full article):
       - Extends frontmatterSchema
       - body: z.string().min(100, 'Body must be at least 100 characters')
    4. Exports validateArticle(data): returns { valid: boolean, errors?: string[] }
    5. Uses .safeParse() for validation
  </action>
  <behavior>
    - frontmatterSchema validates title is required, date is optional valid format
    - articleSchema validates body minimum 100 characters
    - validateArticle returns flat error messages for display
  </behavior>
  <verify>
    <automated>npx vitest run src/lib/content/validator.test.ts</automated>
  </verify>
  <done>
    - Zod schemas exported correctly
    - Validation returns clear error messages
    - Body length validation works
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate parser + validator + slug generation</name>
  <files>src/lib/content/workflow.ts</files>
  <read_first>
    - src/lib/content/parser.ts (from Task 1)
    - src/lib/content/validator.ts (from Task 2)
    - src/lib/mcp/slugify.ts
  </read_first>
  <action>
    Create src/lib/content/workflow.ts that:
    1. Imports parseMarkdown from './parser'
    2. Imports { articleSchema } from './validator'
    3. Imports generateSlug from '../mcp/slugify'
    4. Exports processMarkdown(rawMarkdown) that:
       - Calls parseMarkdown(rawMarkdown)
       - If parse fails, returns error immediately
       - If parse succeeds, validates with articleSchema
       - If validation fails, returns error with details
       - If validation succeeds, generates slug via generateSlug(title)
       - Returns { success: true, data: { meta, body, slug } }
    5. Handles all error cases with clear messages (ERR-01, ERR-02, ERR-05)
  </action>
  <verify>
    <automated>npx vitest run src/lib/content/workflow.test.ts</automated>
  </verify>
  <done>
    - End-to-end workflow function works
    - All validation errors return clear messages
    - Slug generated from title correctly
  </done>
</task>

</tasks>

<verification>
- Parser extracts metadata correctly
- Validator enforces required fields
- Slug generation works from title
- Error messages are clear and actionable
- All tests pass
</verification>

<success_criteria>
- User can submit Markdown with YAML frontmatter
- Claude extracts title, date, tags, excerpt automatically
- System validates title required, body > 100 chars
- Slug generated from title
- Clear errors for: missing title, invalid date format, body too short
</success_criteria>

<output>
After completion, create `.planning/phases/07-content-workflow/07-01-SUMMARY.md`
</output>
