---
phase: 7
plan: 3
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - src/lib/content/workflow.ts
  - src/lib/mcp/db.ts
autonomous: true
requirements:
  - WORK-05
  - ERR-01
  - ERR-02
  - ERR-03
  - ERR-05
must_haves:
  truths:
    - "User can save article as draft for later publishing"
    - "User can list and manage existing drafts"
    - "System returns clear error messages for all failure cases"
  artifacts:
    - path: "src/lib/content/workflow.ts"
      provides: "Draft management workflow functions"
      exports: ["listDrafts", "saveDraft", "discardArticle"]
    - path: "src/lib/mcp/db.ts"
      provides: "Draft listing and management"
      exports: ["listDrafts", "getArticle"]
  key_links:
    - from: "src/lib/content/workflow.ts"
      to: "src/lib/mcp/db.ts"
      via: "listArticles with status filter"
      pattern: "listArticles.*status.*draft"
---

<objective>
Implement draft management and error handling integration.

Purpose: Enable saving articles as drafts, listing existing drafts, and ensure all errors are properly surfaced with clear messages.

Output: Draft workflow functions and error handling integration.
</objective>

<execution_context>
@/Users/wenjiaqi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenjiaqi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-content-workflow/07-CONTEXT.md
@.planning/phases/07-content-workflow/07-RESEARCH.md

# Phase 6 existing code
@src/lib/mcp/db.ts
@src/lib/mcp/server.ts
</context>

<interfaces>
From src/lib/mcp/db.ts:
```typescript
export async function listArticles(options: ListArticlesOptions): Promise<Article[]>
interface ListArticlesOptions {
  status?: string;
  limit?: number;
  offset?: number;
  order_by?: 'date_DESC' | 'date_ASC';
}
```

Expected new exports:
```typescript
export async function listDrafts(): Promise<Article[]>
export async function getArticle(slug: string): Promise<Article | null>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add draft listing to database layer</name>
  <files>src/lib/mcp/db.ts</files>
  <read_first>
    - src/lib/mcp/db.ts
  </read_first>
  <action>
    Add to src/lib/mcp/db.ts:
    1. Export function listDrafts() that calls listArticles({ status: 'draft' })
    2. Ensure listArticles already supports status filter (it does from Phase 6)
  </action>
  <verify>
    <automated>npx vitest run src/lib/mcp/db.test.ts</automated>
  </verify>
  <done>
    - listDrafts function exported
    - Returns all draft articles
  </done>
</task>

<task type="auto">
  <name>Task 2: Add draft management to workflow</name>
  <files>src/lib/content/workflow.ts</files>
  <read_first>
    - src/lib/content/workflow.ts
    - src/lib/mcp/db.ts
  </read_first>
  <action>
    Extend src/lib/content/workflow.ts:
    1. Export async function listDrafts() that:
       - Calls db.listDrafts()
       - Returns { success: true, drafts: Article[] } or { success: false, error }
    2. Export async function saveDraft(meta, body) that:
       - Calls createArticle with status: 'draft'
       - Returns { success: true, article } or { success: false, error }
    3. Export async function discardArticle(slug) that:
       - Calls deleteArticle (soft delete) to discard
       - Returns { success: true } or { success: false, error }
    4. All functions handle errors and return clear error messages
  </action>
  <verify>
    <automated>npx vitest run src/lib/content/workflow.test.ts</automated>
  </verify>
  <done>
    - Draft listing works
    - Save as draft creates article with status: 'draft'
    - Discard removes article
  </done>
</task>

<task type="auto">
  <name>Task 3: Error handling integration</name>
  <files>src/lib/content/workflow.ts</files>
  <read_first>
    - src/lib/content/workflow.ts
  </read_first>
  <action>
    Ensure src/lib/content/workflow.ts has proper error handling:
    1. ERR-01: Title extraction failure → "Missing or invalid frontmatter: title is required"
    2. ERR-02: Slug collision → Handled by createArticle (adds -1, -2 suffix)
    3. ERR-03: Database write failure → "Failed to save article: {error message}"
    4. ERR-05: Markdown format error → "Invalid Markdown format: {specific error}"
    5. Wrap all async operations in try-catch with clear error messages
  </action>
  <verify>
    <automated>npx vitest run src/lib/content/workflow.test.ts</automated>
  </verify>
  <done>
    - All error cases return clear, actionable messages
    - Error messages guide user to fix the issue
  </done>
</task>

</tasks>

<verification>
- Drafts can be listed
- Articles can be saved as draft
- Errors return clear messages
- All tests pass
</verification>

<success_criteria>
- User can save article as draft (WORK-05)
- User can list existing drafts
- Clear error messages for: title extraction failure, slug collision, database write failure, invalid Markdown
</success_criteria>

<output>
After completion, create `.planning/phases/07-content-workflow/07-03-SUMMARY.md`
</output>
