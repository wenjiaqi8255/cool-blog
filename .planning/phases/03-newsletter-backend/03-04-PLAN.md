---
phase: 3
plan: 4
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/pages/api/resend.ts
  - src/lib/email.ts
  - src/components/interactive/SubscribeModal.tsx
autonomous: true
requirements:
  - NEWS-03
  - NEWS-04
nyquist_compliant: true
must_haves:
  truths:
    - "The /api/resend endpoint was correctly implemented by 03-02 with subscriber lookup"
    - "The /api/resend endpoint was correctly implemented by 03-02 with timestamp update and email resend"
    - "The SubscribeModal was correctly wired by 03-02 with resend button and confirmation states"
    - "The SubscribeModal was correctly wired by 03-02 with loading state and success feedback"
  artifacts:
    - path: "src/pages/api/resend.ts"
      provides: "POST endpoint for resending confirmation email (verified: created by 03-02)"
      exports: "POST handler, prerender = false"
      verified_by: "03-02"
    - path: "src/lib/email.ts"
      provides: "Resend email utility with styled HTML template (verified: created by 03-02)"
      exports: "sendConfirmationEmail"
      verified_by: "03-02"
    - path: "src/components/interactive/SubscribeModal.tsx"
      provides: "Success state with resend button, loading, and confirmation feedback (verified: created by 03-02)"
      exports: "handleResendConfirmation, isResending, resendSuccess, subscribedEmail"
      verified_by: "03-02"
  key_links:
    - from: "src/pages/api/resend.ts"
      to: "src/db/index.ts"
      via: "import { db, schema }"
      pattern: "db.*select.*subscribers"
    - from: "src/pages/api/resend.ts"
      to: "src/lib/email.ts"
      via: "import sendConfirmationEmail"
      pattern: "sendConfirmationEmail"
    - from: "src/components/interactive/SubscribeModal.tsx"
      to: "src/pages/api/resend.ts"
      via: "fetch POST /api/resend with subscribedEmail"
      pattern: "fetch.*api/resend"
---

<objective>
Verify that Plan 03-02 correctly implemented the resend flow, then enhance any missing polish or edge cases. This plan does NOT create new artifacts - it verifies and enhances what 03-02 built.
</objective>

<context>
@.planning/phases/03-newsletter-backend/03-RESEARCH.md
@.planning/phases/03-newsletter-backend/03-02-PLAN.md

**State at plan start:**
- Plan 03-01: DB schema, client, and createSubscriber repository exist
- Plan 03-02: SubscribeModal has been wired to API, /api/subscribe and /api/resend endpoints exist, email utility exists

**Duplicate ownership note (FIXED):**
- 03-02 creates the full subscribe flow (SubscribeModal, resend.ts, email.ts)
- This plan (03-04) ONLY verifies and enhances - does NOT claim ownership

**Unique enhancements this plan can add (if not already present):**
- Additional validation edge cases in resend endpoint (e.g., rate limiting)
- Additional error handling in email utility (e.g., retry logic)
- Additional UI polish in SubscribeModal (e.g., animations)

**Key decisions (locked):**
- Store subscribed email so resend uses it automatically (no re-entry needed)
- Resend updates confirmationSentAt timestamp in DB
- Return 404 if email not found (security: do not reveal if email is in subscriber list)
- Email failure is logged but does not fail the resend request
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify /api/resend endpoint exists and is correctly implemented by 03-02</name>
  <files>src/pages/api/resend.ts</files>
  <read_first>src/pages/api/resend.ts</read_first>
  <verify>
    <automated>grep -l "prerender.*false" src/pages/api/resend.ts && echo "OK"</automated>
  </verify>
  <action>
Read src/pages/api/resend.ts and verify that Plan 03-02 correctly implemented it with all required functionality.

Verify the file exists AND includes:
- `export const prerender = false`
- Zod validation for email
- Subscriber lookup with `eq(schema.subscribers.email, email)`
- `limit(1)` and destructuring `[subscriber]`
- 404 response when subscriber not found
- `db.update()` to set confirmationSentAt
- `sendConfirmationEmail(email)` call
- JSON response with success/error

If the file is missing or incomplete, report this as a verification failure (not a creation task - 03-02 should have created it).

This is a VERIFICATION task - 03-02 owns this artifact.
  </action>
  <done>POST /api/resend endpoint correctly implemented by 03-02 with subscriber lookup, timestamp update, and email resend</done>
</task>

<task type="auto">
  <name>Task 2: Verify email template has styled HTML</name>
  <files>src/lib/email.ts</files>
  <read_first>src/lib/email.ts</read_first>
  <verify>
    <automated>grep -c "style=" src/lib/email.ts</automated>
  </verify>
  <action>
Read src/lib/email.ts and verify the email template has proper styled inline HTML.

Verify:
- File exists
- `sendConfirmationEmail` function exported
- HTML template uses inline styles (count of `style=` attributes)
- HTML uses table-based layout for email client compatibility
- HTML has DOCTYPE and charset

If the file is missing or the template lacks inline styles, report as verification failure (03-02 should have created it with styles).

This is a VERIFICATION task - 03-02 owns this artifact.
  </action>
  <done>src/lib/email.ts has styled HTML email template (verified created by 03-02)</done>
</task>

<task type="auto">
  <name>Task 3: Verify SubscribeModal resend functionality exists</name>
  <files>src/components/interactive/SubscribeModal.tsx</files>
  <read_first>src/components/interactive/SubscribeModal.tsx</read_first>
  <verify>
    <automated>grep -c "isResending\|resendSuccess\|subscribedEmail" src/components/interactive/SubscribeModal.tsx</automated>
  </verify>
  <action>
Read the current SubscribeModal.tsx and verify 03-02 correctly wired the resend functionality.

Verify the following state variables and functions exist:
1. `subscribedEmail` state variable to store the email on success
2. `handleResendConfirmation` function that POSTs to /api/resend
3. Success state rendering with resend button
4. `isResending` loading state during API call
5. `resendSuccess` state for feedback after successful resend

If any of these are missing, report as verification failure (03-02 should have implemented them).

This is a VERIFICATION task - 03-02 owns this artifact.
  </action>
  <done>SubscribeModal has resend confirmation button with loading and success feedback (verified wired by 03-02)</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Verify /api/resend endpoint exists with prerender = false: `grep "prerender.*false" src/pages/api/resend.ts`
2. Verify email has styled HTML template: `grep "style=" src/lib/email.ts | wc -l` (should be > 5)
3. Verify SubscribeModal has resend button: `grep "Resend confirmation" src/components/interactive/SubscribeModal.tsx`
4. Verify resend has loading state: `grep "isResending" src/components/interactive/SubscribeModal.tsx`
</verification>

<success_criteria>
- [ ] Verified /api/resend endpoint exists with correct implementation from 03-02
- [ ] Verified email template has styled inline HTML from 03-02
- [ ] Verified SubscribeModal has resend button with loading and success feedback from 03-02
- [ ] NEWS-03 (confirmation message) and NEWS-04 (resend email) verified as implemented by 03-02
</success_criteria>

<output>
After completion, create `.planning/phases/03-newsletter-backend/03-04-SUMMARY.md`
</output>