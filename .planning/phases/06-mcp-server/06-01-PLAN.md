---
phase: 06-mcp-server
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/mcp/slugify.ts
  - src/lib/mcp/server.ts
  - src/pages/api/mcp.ts
autonomous: true
requirements:
  - MCP-05
  - MCP-06

must_haves:
  truths:
    - "API key validation occurs before any MCP tool execution"
    - "Slug generation works for English and Chinese article titles"
    - "MCP server accepts requests at /api/mcp endpoint"
  artifacts:
    - path: src/lib/mcp/slugify.ts
      provides: URL-safe slug generation with Chinese transliteration
    - path: src/lib/mcp/server.ts
      provides: MCP server with 4 tool definitions and Zod validation
    - path: src/pages/api/mcp.ts
      provides: HTTP endpoint with API key authentication
  key_links:
    - from: src/pages/api/mcp.ts
      to: process.env.MCP_API_KEY
      via: Bearer token validation
    - from: src/lib/mcp/server.ts
      to: src/lib/mcp/slugify.ts
      via: import for slug generation in create_article
    - from: MCP tools
      to: src/db/schema.ts
      via: Drizzle queries (deferred to Plan 2)
---

<objective>
Set up MCP server foundation with authentication and slug generation utility.

Purpose: Establish core infrastructure for MCP server - API key validation middleware, slug generation for Chinese characters, and HTTP transport endpoint. Enables subsequent plan to implement article CRUD tools.

Output:
- MCP server endpoint at /api/mcp with API key auth
- Slug generation utility with Chinese transliteration
- MCP server configuration
</objective>

<context>
@.planning/REQUIREMENTS.md (MCP-05, MCP-06)
@.planning/phases/06-mcp-server/06-CONTEXT.md (decisions: HTTP transport, API key auth, slug auto-generation)
@.planning/phases/06-mcp-server/06-RESEARCH.md (MCP SDK patterns, slug algorithm)
@src/db/schema.ts (articles table with slug, status, deleted_at)
@src/pages/api/subscribe.ts (API route pattern with Zod validation)

## MCP Requirements to Address
- MCP-05: API key authentication via Authorization: Bearer <API_KEY>
- MCP-06: Zod schemas for all tool inputs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create slug generation utility with Chinese transliteration</name>
  <files>src/lib/mcp/slugify.ts</files>
  <read_first>
    - src/db/schema.ts (to confirm articles.slug column exists)
  </read_first>
  <action>
Create src/lib/mcp/slugify.ts with generateSlug function that:
1. Converts title to lowercase
2. Normalizes Unicode (NFD) and removes diacritics
3. Replaces non-word characters (except spaces/hyphens) with empty string
4. Replaces spaces with hyphens
5. Collapses multiple hyphens into one
6. Returns slug-safe string

Export type: (title: string) => string

Example:
- "Hello World" → "hello-world"
- "我的文章" → "wo-de-wen-zhang"
- "Hello, World! Test" → "hello-world-test"
  </action>
  <verify>
<automated>grep -n "export function generateSlug" src/lib/mcp/slugify.ts && node -e "import('./src/lib/mcp/slugify.ts').then(m => console.log(m.generateSlug('Hello World')))"</automated>
  </verify>
  <done>
- generateSlug function exists in src/lib/mcp/slugify.ts
- Function handles: lowercase, diacritic removal, space-to-hyphen, multi-hyphen collapse
- Function handles Chinese characters via Unicode normalization
- Type exported: (title: string) => string
  </done>
  <acceptance_criteria>
- [ ] `src/lib/mcp/slugify.ts` file created
- [ ] `generateSlug` function exported from file
- [ ] Function handles ASCII titles: "Hello World" → "hello-world"
- [ ] Function handles Chinese: "测试文章" → transliterated ASCII
- [ ] Function collapses multiple hyphens: "a---b" → "a-b"
- [ ] TypeScript types work without errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create MCP server configuration</name>
  <files>src/lib/mcp/server.ts</files>
  <read_first>
    - src/lib/mcp/slugify.ts (will be created in Task 1)
  </read_first>
  <action>
Create src/lib/mcp/server.ts that:
1. Imports McpServer from '@modelcontextprotocol/server'
2. Creates McpServer instance with name 'cool-blog', version '1.0.0'
3. Registers all 4 tools with Zod schemas:
   - create_article: title (required), body (required), date (optional), tags (optional), excerpt (optional), status (optional, default 'draft')
   - list_articles: status (optional), limit (optional, default 20), offset (optional, default 0), order_by (optional, default 'date_DESC')
   - get_article: slug (required)
   - delete_article: slug (required)
4. Each tool handler calls corresponding database function (imported from lib/mcp/db.ts - will be in Plan 2)
5. Returns tool result as JSON in MCP content format

Use exact Zod schemas from RESEARCH.md:
- createArticleSchema: title.z.string().min(1), body.z.string().min(1), date.z.string().datetime().optional(), tags.z.array(z.string()).optional(), excerpt.z.string().optional(), status.z.enum(['draft', 'published']).optional()
- listArticlesSchema: status.z.enum(['draft', 'published']).optional(), limit.z.number().int().min(1).max(100).optional(), offset.z.number().int().min(0).optional(), order_by.z.enum(['date_DESC', 'date_ASC']).optional()
- getArticleSchema: slug.z.string().min(1)
- deleteArticleSchema: slug.z.string().min(1)
  </action>
  <verify>
<automated>grep -n "registerTool" src/lib/mcp/server.ts | head -10</automated>
  </verify>
  <done>
- McpServer instance created with name 'cool-blog'
- All 4 tools registered with Zod schemas (MCP-06 satisfied)
- Tool handlers await database functions and return MCP format
- File exports: createMcpServer() function
  </done>
  <acceptance_criteria>
- [ ] `src/lib/mcp/server.ts` file created
- [ ] McpServer imported from '@modelcontextprotocol/server'
- [ ] create_article tool registered with title, body required fields
- [ ] list_articles tool registered with status, limit, offset, order_by params
- [ ] get_article tool registered with slug required
- [ ] delete_article tool registered with slug required
- [ ] All input validated via Zod schemas (MCP-06)
- [ ] File exports createMcpServer() function
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create MCP API endpoint with authentication</name>
  <files>src/pages/api/mcp.ts</files>
  <read_first>
    - src/pages/api/subscribe.ts (API route pattern)
    - src/lib/mcp/server.ts (will be created in Task 2)
  </read_first>
  <action>
Create src/pages/api/mcp.ts as APIRoute handler that:
1. Validates Authorization header: must be "Bearer <token>"
2. Extracts token and compares to process.env.MCP_API_KEY
3. Returns 401 if no header, 403 if invalid token
4. If auth passes, processes MCP request via createMcpServer() and NodeHttpTransport
5. Returns MCP response with proper Content-Type

Use exact auth check from RESEARCH.md pattern:
```typescript
const authHeader = request.headers.get('Authorization');
if (!authHeader?.startsWith('Bearer ')) {
  return new Response('Unauthorized', { status: 401 });
}
const token = authHeader.slice(7);
if (token !== process.env.MCP_API_KEY) {
  return new Response('Invalid API key', { status: 403 });
}
```

Handle GET and POST methods (MCP protocol requires both).
  </action>
  <verify>
<automated>grep -n "MCP_API_KEY" src/pages/api/mcp.ts && grep -n "Bearer" src/pages/api/mcp.ts</automated>
  </verify>
  <done>
- APIRoute handler created at src/pages/api/mcp.ts
- API key validation on every request (MCP-05 satisfied)
- Returns 401 for missing Authorization, 403 for invalid key
- MCP-01-04 tools accessible after auth passes
- Endpoint ready for deployment to /api/mcp
  </done>
  <acceptance_criteria>
- [ ] `src/pages/api/mcp.ts` file created
- [ ] POST handler processes MCP requests
- [ ] GET handler available (MCP protocol support)
- [ ] 401 returned when Authorization header missing
- [ ] 403 returned when token doesn't match MCP_API_KEY
- [ ] MCP server processes authenticated requests
- [ ] Content-Type: application/json or protocol-compliant
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] MCP-05 satisfied: API key authentication on all requests
- [ ] MCP-06 satisfied: Zod schemas validate all tool inputs
- [ ] Endpoint accessible at /api/mcp after deployment
- [ ] 401/403 errors returned for invalid/missing auth
</verification>

<success_criteria>
MCP server foundation complete:
1. API key auth validates every request before tool execution
2. Slug generation handles both ASCII and Chinese titles
3. MCP server accepts HTTP requests at /api/mcp
4. All 4 tools registered with Zod validation schemas
</success_criteria>

<output>
After completion, create `.planning/phases/06-mcp-server/06-01-SUMMARY.md`
</output>