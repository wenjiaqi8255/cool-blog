---
phase: 02-content-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/config.ts
  - src/content/articles/.gitkeep
  - package.json
autonomous: true
requirements:
  - ART-01
  - ART-02
  - ART-03
must_haves:
  truths:
    - "Content Collection API provides type-safe article access"
    - "Articles can be queried with getCollection('articles')"
    - "Schema validation prevents invalid frontmatter"
  artifacts:
    - path: "src/content/config.ts"
      provides: "Content Collection schema definition"
      exports: ["collections", "articles"]
    - path: "src/content/articles/hello-world.md"
      provides: "Sample article for testing"
      contains: "frontmatter with title, date, tags"
  key_links:
    - from: "src/content/config.ts"
      to: "astro:content"
      via: "defineCollection import"
      pattern: "defineCollection.*z\\.object"
    - from: "ArticleCard.astro"
      to: "src/content/config.ts"
      via: "CollectionEntry type"
      pattern: "CollectionEntry.*articles"
---

<objective>
Set up Astro Content Collections with Zod schema for articles. This creates the foundation for all article management - type-safe frontmatter validation, markdown content, and collection API.
</objective>

<execution_context>
@/Users/wenjiaqi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenjiaqi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-content-system/02-CONTEXT.md
@.planning/phases/02-content-system/02-RESEARCH.md

From src/content/config.ts pattern (to be created):
- Define articles collection with Zod schema
- Schema fields: title (string), date (date), tags (enum: ML, Systems, Tutorial, Project, Notes), excerpt (optional), coverImage (optional), draft (optional)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies (zod, fuse.js)</name>
  <files>package.json</files>
  <read_first>/Users/wenjiaqi/Downloads/cool-blog/package.json</read_first>
  <action>
Add zod and fuse.js dependencies to package.json:
- zod: ^3.x (for schema validation)
- fuse.js: latest (for client-side search)

Run: npm install zod fuse.js
  </action>
  <verify>
<automated>grep -q '"zod"' package.json && grep -q '"fuse.js"' package.json</automated>
  </verify>
  <done>zod and fuse.js installed in node_modules</done>
</task>

<task type="auto">
  <name>Task 2: Create Content Collection config</name>
  <files>src/content/config.ts</files>
  <read_first>
/Users/wenjiaqi/Downloads/cool-blog/astro.config.mjs
/Users/wenjiaqi/Downloads/cool-blog/.planning/phases/02-content-system/02-CONTEXT.md (lines 26-33: frontmatter schema)
</read_first>
  <action>
Create src/content/config.ts with Zod schema:

```typescript
import { defineCollection, z } from 'astro:content';

const articles = defineCollection({
  type: 'content',
  schema: z.object({
    title: z.string().min(1),
    date: z.date(),
    tags: z.array(z.enum(['ML', 'Systems', 'Tutorial', 'Project', 'Notes'])),
    excerpt: z.string().optional(),
    coverImage: z.string().optional(),
    draft: z.boolean().optional(),
  }),
});

export const collections = { articles };
```
  </action>
  <verify>
<automated>node -e "import('./src/content/config.ts').then(m => console.log(Object.keys(m.collections)))"</automated>
  </verify>
  <done>Content Collection 'articles' defined with Zod validation</done>
</task>

<task type="auto">
  <name>Task 3: Create sample article for testing</name>
  <files>src/content/articles/hello-world.md</files>
  <read_first>/Users/wenjiaqi/Downloads/cool-blog/.planning/phases/02-content-system/02-RESEARCH.md (lines 213-225: article frontmatter example)</read_first>
  <action>
Create sample article at src/content/articles/hello-world.md:

```markdown
---
title: "理解 Transformer 注意力机制"
date: 2026-03-28
tags: ["ML", "Tutorial"]
excerpt: "深入理解 Transformer 模型中的注意力机制，这是现代大语言模型的核心组件。"
draft: false
---

# 理解 Transformer 注意力机制

注意力机制是 Transformer 模型的核心创新。它允许模型在处理序列时关注相关的部分。

## 基本概念

注意力函数可以描述为将一个查询和一组键值对映射到一个输出。

\`\`\`python
import torch
import torch.nn.functional as F

def scaled_dot_product_attention(Q, K, V, mask=None):
    d_k = Q.size(-1)
    scores = torch.matmul(Q, K.transpose(-2, -1)) / math.sqrt(d_k)
    if mask is not None:
        scores = scores.masked_fill(mask == 0, -1e9)
    attention = F.softmax(scores, dim=-1)
    return torch.matmul(attention, V)
\`\`\`

## 为什么需要缩放

缩放因子 √d_k 用于防止点积结果过大，导致 softmax 函数进入梯度极小的区域。
```
  </action>
  <verify>
<automated>ls -la src/content/articles/hello-world.md</automated>
  </verify>
  <done>Sample article created with frontmatter, markdown body, and code block</done>
</task>

</tasks>

<verification>
- Run `npm run build` to verify Content Collection compiles without errors
- Verify schema validation works: create article with invalid tag should fail build
</verification>

<success_criteria>
- Content Collection config exists with Zod schema
- Sample article can be queried via getCollection('articles')
- Build succeeds with content collection
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-system/02-01-SUMMARY.md`
</output>