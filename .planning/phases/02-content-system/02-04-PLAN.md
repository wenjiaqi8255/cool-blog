---
phase: 02-content-system
plan: 04
type: execute
wave: 4
depends_on:
  - 02-02
files_modified:
  - src/components/articles/TagFilter.tsx
  - src/components/articles/SearchInput.tsx
  - src/components/articles/ArticleList.astro
  - src/lib/search.ts
  - src/pages/articles/index.astro
autonomous: true
requirements:
  - ART-05
must_haves:
  truths:
    - "User can filter articles by tag (OR logic)"
    - "User can search articles by title/content using fuzzy search"
    - "Infinite scroll loads more articles as user scrolls"
    - "No results shows '没有找到匹配的文章'"
  artifacts:
    - path: "src/components/articles/TagFilter.tsx"
      provides: "Tag pill buttons for filtering"
      exports: ["TagFilter"]
    - path: "src/components/articles/SearchInput.tsx"
      provides: "Search input with debounce"
      exports: ["SearchInput"]
    - path: "src/lib/search.ts"
      provides: "Fuse.js search utility"
      exports: ["createSearchIndex", "search"]
    - path: "src/components/articles/ArticleList.astro"
      provides: "Article list with search and filter integration"
      contains: "没有找到匹配的文章"
  key_links:
    - from: "TagFilter.tsx"
      to: "ArticleList.astro"
      via: "onChange callback updates selectedTags state"
      pattern: "selectedTags.*onChange"
    - from: "SearchInput.tsx"
      to: "src/lib/search.ts"
      via: "onSearch callback triggers Fuse.js search"
      pattern: "onSearch.*search"
    - from: "ArticleList.astro"
      to: "src/content/config.ts"
      via: "getCollection provides articles for filtering"
      pattern: "getCollection.*articles"
---

<objective>
Implement client-side search and tag filter functionality with infinite scroll. Users can filter articles by tags (OR logic) and search by title/content using Fuse.js.
</objective>

<execution_context>
@/Users/wenjiaqi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenjiaqi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-content-system/02-CONTEXT.md
@.planning/phases/02-content-system/02-UI-SPEC.md

From CONTEXT.md:
- Filter: Tag pill buttons at top of article list
- Tag logic: OR — articles with ANY selected tag
- Search: Client-side with Fuse.js, search title and content
- Loading: Infinite scroll
- No results: "没有找到匹配的文章"

From UI-SPEC.md:
- Search placeholder: "搜索文章标题或内容..."
- Tag filter label: "按标签筛选"
- Tag set: ML, Systems, Tutorial, Project, Notes
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TagFilter component</name>
  <files>src/components/articles/TagFilter.tsx</files>
  <read_first>/Users/wenjiaqi/Downloads/cool-blog/src/components/interactive/TabNavigation.tsx</read_first>
  <action>
Create TagFilter.tsx React component:

- Props: availableTags: string[], selectedTags: string[], onChange: (tags: string[]) => void
- Fixed tag set: ['ML', 'Systems', 'Tutorial', 'Project', 'Notes']
- Display: pill buttons with label "按标签筛选" above
- States:
  - Default: outline style (border #111, transparent bg)
  - Selected: filled #111 background, white text
- Logic: OR — clicking tag toggles it, show articles matching ANY selected tag
- If no tags selected: show all articles
  </action>
  <verify>
<automated>grep -q "TagFilter" src/components/articles/TagFilter.tsx && grep -q "按标签筛选" src/components/articles/TagFilter.tsx</automated>
  </verify>
  <done>TagFilter component with pill buttons and OR logic</done>
</task>

<task type="auto">
  <name>Task 2: Create SearchInput component</name>
  <files>src/components/articles/SearchInput.tsx</files>
  <read_first>/Users/wenjiaqi/Downloads/cool-blog/.planning/phases/02-content-system/02-RESEARCH.md (lines 152-165: Fuse.js example)</read_first>
  <action>
Create SearchInput.tsx React component:

- Props: onSearch: (query: string) => void
- Input: text field with placeholder "搜索文章标题或内容..."
- Debounce: 300ms delay before triggering search
- Clear button: X icon to clear search query
- Styling: matches site design (border, focus states)
- Use Fuse.js for client-side search
  </action>
  <verify>
<automated>grep -q "SearchInput" src/components/articles/SearchInput.tsx && grep -q "搜索文章" src/components/articles/SearchInput.tsx</automated>
  </verify>
  <done>SearchInput component with debounced search</done>
</task>

<task type="auto">
  <name>Task 3: Create search utility with Fuse.js</name>
  <files>src/lib/search.ts</files>
  <read_first>/Users/wenjiaqi/Downloads/cool-blog/package.json</read_first>
  <action>
Create src/lib/search.ts:

- Import Fuse from fuse.js
- Create search index from article metadata
- Search index includes: slug, title, excerpt, content (raw body), tags
- Fuse.js options:
  - keys: ['title', 'excerpt', 'content', 'tags']
  - threshold: 0.3 (fuzzy matching)
  - includeScore: true
- Export: createSearchIndex(articles), search(index, query)
  </action>
  <verify>
<automated>grep -q "Fuse" src/lib/search.ts && grep -q "createSearchIndex" src/lib/search.ts</automated>
  </verify>
  <done>Search utility with Fuse.js ready</done>
</task>

<task type="auto">
  <name>Task 4: Update ArticleList with search and filter</name>
  <files>src/components/articles/ArticleList.astro</files>
  <read_first>/Users/wenjiaqi/Downloads/cool-blog/.planning/phases/02-content-system/02-CONTEXT.md (lines 39-40: empty state text)</read_first>
  <action>
Update ArticleList.astro to include search and filter:

- Add client-side state for: selectedTags[], searchQuery, filteredArticles
- Integrate TagFilter component at top of list
- Integrate SearchInput component alongside tag filters
- Apply filters:
  - Tag filter: OR logic, show articles with ANY selected tag
  - Search: fuzzy match on title, excerpt, content using Fuse.js
- Infinite scroll: load initial batch (e.g., 6 articles), load more on scroll
- Empty state: "没有找到匹配的文章" (no results message)
- Display article count: "{N} 篇文章"
  </action>
  <verify>
<automated>grep -q "没有找到匹配的文章" src/components/articles/ArticleList.astro || grep -q "TagFilter" src/components/articles/ArticleList.astro</automated>
  </verify>
  <done>ArticleList supports tag filtering, search, and infinite scroll</done>
</task>

</tasks>

<verification>
- Run `npm run build` to verify components compile
- Test tag filtering: select ML tag, verify filtered results
- Test search: type query, verify fuzzy search results
- Test no results: search for non-existent term, verify empty state
</verification>

<success_criteria>
- Tag pill buttons filter articles with OR logic
- Search input performs fuzzy search on title/content
- Infinite scroll loads more articles on scroll
- "没有找到匹配的文章" shown when no results
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-system/02-04-SUMMARY.md`
</output>