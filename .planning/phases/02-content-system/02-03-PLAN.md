---
phase: 02-content-system
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - astro.config.mjs
  - src/components/articles/CodeBlock.astro
  - src/styles/global.css
autonomous: true
requirements:
  - ART-04
must_haves:
  truths:
    - "User can see code blocks with syntax highlighting in articles"
    - "Code blocks match Terminal card aesthetic (#111 background)"
    - "Copy button available on every code block"
  artifacts:
    - path: "astro.config.mjs"
      provides: "Shiki configuration for syntax highlighting"
      contains: "shikiConfig with dark theme"
    - path: "src/components/articles/CodeBlock.astro"
      provides: "Copy button functionality for code blocks"
      contains: "navigator.clipboard writeText"
    - path: "src/styles/global.css"
      provides: "Code block styling matching Terminal card"
      contains: "pre { background: #111 }"
  key_links:
    - from: "astro.config.mjs"
      to: "Shiki"
      via: "markdown.shikiConfig"
      pattern: "shikiConfig.*theme"
    - from: "src/pages/articles/[slug].astro"
      to: "CodeBlock.astro"
      via: "Article content rendered with code blocks"
      pattern: "pre.*code"
---

<objective>
Configure Shiki syntax highlighting with Terminal card aesthetic (#111 background, monochrome colors) and add copy button to all code blocks.
</objective>

<execution_context>
@/Users/wenjiaqi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenjiaqi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-content-system/02-CONTEXT.md
@.planning/phases/02-content-system/02-RESEARCH.md

From CONTEXT.md:
- Code blocks: match Terminal card style — dark background (#111), monochrome colors
- Code block features: Copy button on every code block

From RESEARCH.md:
- Shiki is built into Astro, zero-config
- Theme should match Terminal card aesthetic

From UI-SPEC.md:
- Code font: JetBrains Mono, 14px, 400 weight
- Code block background: #111
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Shiki theme in Astro config</name>
  <files>astro.config.mjs</files>
  <read_first>
/Users/wenjiaqi/Downloads/cool-blog/astro.config.mjs
/Users/wenjiaqi/Downloads/cool-blog/.planning/phases/02-content-system/02-RESEARCH.md (lines 196-201: Shiki theme pitfall)
</read_first>
  <action>
Update astro.config.mjs to configure Shiki:

```javascript
markdown: {
  shikiConfig: {
    theme: 'github-dark', // or 'min-dark', matches #111 aesthetic
    wrap: true, // wrap long lines
  }
}
```

Note: Use built-in dark theme that closely matches Terminal card #111 background. If exact match needed, custom theme can be added later.
  </action>
  <verify>
<automated>grep -q "shikiConfig" astro.config.mjs</automated>
  </verify>
  <done>Shiki configured with dark theme in Astro config</done>
</task>

<task type="auto">
  <name>Task 2: Create CodeBlock wrapper component with copy button</name>
  <files>src/components/articles/CodeBlock.astro</files>
  <read_first>/Users/wenjiaqi/Downloads/cool-blog/.planning/phases/02-content-system/02-RESEARCH.md (lines 270-293: copy button example)</read_first>
  <action>
Create CodeBlock.astro component that wraps code blocks and adds copy functionality:

- Component wraps Shiki-generated code blocks
- Add copy button positioned top-right of each pre tag
- Button states: default "Copy", clicked "Copied!" (reverts after 2s)
- Copy functionality using navigator.clipboard API
- Style: button with subtle background, matches Terminal card aesthetic

Implementation: Use client-side script that:
1. Finds all pre tags in article content
2. Injects copy button into each
3. Attaches click handler for clipboard copy
  </action>
  <verify>
<automated>grep -q "navigator.clipboard" src/components/articles/CodeBlock.astro || grep -q "copy-button" src/components/articles/CodeBlock.astro</automated>
  </verify>
  <done>Code block component with copy functionality exists</done>
</task>

<task type="auto">
  <name>Task 3: Add code block styling to global.css</name>
  <files>src/styles/global.css</files>
  <read_first>/Users/wenjiaqi/Downloads/cool-blog/src/styles/global.css</read_first>
  <action>
Add code block styles to global.css matching Terminal card aesthetic:

```css
/* Code blocks - Terminal card aesthetic */
pre {
  background: #111 !important;
  border-radius: 8px;
  padding: 16px;
  overflow-x: auto;
  font-family: 'JetBrains Mono', monospace;
  font-size: 14px;
  line-height: 1.5;
}

code {
  font-family: 'JetBrains Mono', monospace;
  font-size: 14px;
}

/* Code block copy button */
.code-block-wrapper {
  position: relative;
}

.copy-button {
  position: absolute;
  top: 8px;
  right: 8px;
  background: rgba(255, 255, 255, 0.1);
  border: 1px solid rgba(255, 255, 255, 0.2);
  color: #888;
  padding: 4px 8px;
  border-radius: 4px;
  font-size: 12px;
  cursor: pointer;
  transition: all 0.2s ease;
}

.copy-button:hover {
  background: rgba(255, 255, 255, 0.2);
  color: #fff;
}
```
  </action>
  <verify>
<automated>grep -q "copy-button" src/styles/global.css && grep -q "JetBrains Mono" src/styles/global.css</automated>
  </verify>
  <done>Code block styles added to global.css</done>
</task>

</tasks>

<verification>
- Run `npm run build` to verify Shiki highlighting works
- Visit article with code blocks to verify styling and copy button
</verification>

<success_criteria>
- Code blocks have #111 dark background
- Syntax highlighting visible in code blocks
- Copy button appears on hover, copies code to clipboard
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-system/02-03-SUMMARY.md`
</output>