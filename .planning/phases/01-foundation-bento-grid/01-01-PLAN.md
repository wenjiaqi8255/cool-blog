---
phase: 01-foundation-bento-grid
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - astro.config.mjs
  - tsconfig.json
  - src/styles/global.css
  - src/layouts/BaseLayout.astro
  - vitest.config.ts
  - playwright.config.ts
  - src/tests/setup.ts
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03
user_setup: []

must_haves:
  truths:
    - "Developer can run `npm run dev` and see Astro dev server start"
    - "Developer can run `npm run build` and generate static output"
    - "Developer can run `npm run test:unit` and see Vitest execute tests"
    - "Tailwind CSS classes generate expected styles"
    - "BaseLayout renders with correct font families loaded"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: '"astro", "tailwindcss", "vitest"'
    - path: "astro.config.mjs"
      provides: "Astro configuration with Cloudflare adapter"
      exports: ["defineConfig"]
      contains: "@astrojs/cloudflare"
    - path: "src/styles/global.css"
      provides: "Tailwind CSS theme configuration"
      contains: "@theme"
    - path: "vitest.config.ts"
      provides: "Vitest test configuration"
      contains: "getViteConfig"
  key_links:
    - from: "astro.config.mjs"
      to: "@astrojs/cloudflare"
      via: "adapter import"
      pattern: "adapter.*cloudflare"
    - from: "src/styles/global.css"
      to: "Tailwind CSS"
      via: "@import"
      pattern: "@import.*tailwindcss"
---

<objective>
Scaffold the Astro project with Cloudflare adapter, Tailwind CSS v4, React integration, and test infrastructure. This creates the foundation that all subsequent card and layout components will build upon.

Purpose: Establish a working development environment with proper framework configuration before building UI components.
Output: Functional Astro project with dev server, build pipeline, and test framework ready.
</objective>

<execution_context>
@/Users/wenjiaqi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenjiaqi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-bento-grid/01-CONTEXT.md
@.planning/phases/01-foundation-bento-grid/01-RESEARCH.md
@.planning/phases/01-foundation-bento-grid/01-UI-SPEC.md

<interfaces>
<!-- Key types and interfaces from RESEARCH.md that executors need -->

From RESEARCH.md - Tailwind v4 Configuration:
```css
@theme {
  --color-canvas: #FFFFFF;
  --color-card-light: #F7F7F7;
  --color-card-hover: #F0F0F0;
  --color-ink-black: #111111;
  --color-ink-gray: #666666;
  --color-ink-light: #999999;
  --font-family-sans: 'Inter', sans-serif;
  --font-family-mono: 'JetBrains Mono', monospace;
  --spacing-card-gap: 4px;
  --spacing-container-padding: 40px;
}
```

From RESEARCH.md - Card Config Interface:
```typescript
interface CardConfig {
  id: string;
  type: 'image' | 'text' | 'terminal' | 'stats';
  span: 1 | 2 | 4;
  row?: 2;
  variant?: 'light' | 'dark';
  props: Record<string, any>;
}
```

From UI-SPEC.md - Typography:
- Sans: Inter (weights 300, 400)
- Mono: JetBrains Mono (weights 300, 400)
- Headlines: -0.02em to -0.04em letter-spacing
- Meta tags: 9px uppercase, 0.1em letter-spacing

From UI-SPEC.md - Spacing Scale:
- xs: 4px (grid gaps)
- sm: 8px
- md: 16px (card padding)
- lg: 24px
- xl: 32px
- 2xl: 48px
- 3xl: 64px
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Astro project with Cloudflare adapter</name>
  <files>package.json, astro.config.mjs, tsconfig.json</files>
  <read_first>
    - .planning/phases/01-foundation-bento-grid/01-RESEARCH.md (for version numbers and configuration patterns)
  </read_first>
  <behavior>
    - npm init creates valid package.json with Astro dependencies
    - astro.config.mjs exports valid configuration
    - tsconfig.json has strict mode enabled
  </behavior>
  <action>
Initialize the Astro project with the following exact configuration:

1. Create `package.json` with these dependencies (use exact versions):
```json
{
  "name": "cool-blog",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "vitest run",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "astro": "^6.1.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "@astrojs/cloudflare": "^13.1.4",
    "@astrojs/react": "^5.0.2",
    "@astrojs/tailwind": "^6.0.2",
    "tailwindcss": "^4.2.2",
    "@fontsource/inter": "^5.0.0",
    "@fontsource/jetbrains-mono": "^5.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "vitest": "^3.0.0",
    "@playwright/test": "^1.50.0",
    "@testing-library/react": "^16.0.0"
  }
}
```

2. Run `npm install` to install dependencies.

3. Create `astro.config.mjs` with this exact content:
```javascript
import { defineConfig } from 'astro/config';
import cloudflare from '@astrojs/cloudflare';
import react from '@astrojs/react';
import tailwind from '@astrojs/tailwind';

export default defineConfig({
  output: 'static',
  adapter: cloudflare({
    platformProxy: {
      enabled: true
    }
  }),
  integrations: [
    react(),
    tailwind()
  ],
  vite: {
    ssr: {
      noExternal: ['@fontsource/inter', '@fontsource/jetbrains-mono']
    }
  }
});
```

4. Create `tsconfig.json` with strict TypeScript configuration:
```json
{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "jsx": "react-jsx",
    "jsxImportSource": "react",
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  }
}
```

IMPORTANT: Do NOT use `npm create astro@latest` interactive command. Create files manually to ensure exact configuration.
  </action>
  <verify>
    <automated>test -f package.json && grep -q '"astro".*"\\^6.1.1"' package.json && grep -q '@astrojs/cloudflare' package.json</automated>
  </verify>
  <done>
    - package.json exists with Astro 6.1.1, React 19, Tailwind 4.2.2, Cloudflare adapter 13.1.4
    - astro.config.mjs exports valid configuration with Cloudflare adapter
    - tsconfig.json has strict mode and React JSX configuration
    - npm install completed without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind CSS v4 with custom theme</name>
  <files>src/styles/global.css</files>
  <read_first>
    - .planning/phases/01-foundation-bento-grid/01-UI-SPEC.md (for color palette and typography specs)
  </read_first>
  <behavior>
    - global.css imports Tailwind CSS
    - @theme block defines all custom tokens
    - Font families are correctly configured
  </behavior>
  <action>
Create Tailwind CSS v4 configuration with the exact design tokens from UI-SPEC.md.

Create `src/styles/global.css` with this exact content:
```css
@import "tailwindcss";

/* Design tokens from UI-SPEC.md */
@theme {
  /* Colors */
  --color-canvas-white: #FFFFFF;
  --color-card-light: #F7F7F7;
  --color-card-hover: #F0F0F0;
  --color-ink-black: #111111;
  --color-ink-gray: #666666;
  --color-ink-light: #999999;
  --color-dark-card-bg: #111111;
  --color-dark-card-hover: #000000;

  /* Typography */
  --font-family-sans: 'Inter', system-ui, sans-serif;
  --font-family-mono: 'JetBrains Mono', monospace;

  /* Spacing (multiples of 4) */
  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 16px;
  --spacing-lg: 24px;
  --spacing-xl: 32px;
  --spacing-2xl: 48px;
  --spacing-3xl: 64px;

  /* Layout */
  --spacing-card-gap: 4px;
  --spacing-container-padding: 40px;
  --layout-max-width: 1600px;
  --layout-min-card-height: 320px;

  /* Animation */
  --transition-timing: cubic-bezier(0.4, 0, 0.2, 1);
  --transition-duration: 300ms;
}

/* Base styles */
body {
  font-family: var(--font-family-sans);
  background-color: var(--color-canvas-white);
  color: var(--color-ink-black);
  font-weight: 400;
  line-height: 1.5;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

/* Typography utilities */
.font-mono {
  font-family: var(--font-family-mono);
}

.tracking-tight {
  letter-spacing: -0.02em;
}

.tracking-tighter {
  letter-spacing: -0.04em;
}

/* Touch target minimum (44px for accessibility) */
.touch-target {
  min-width: 44px;
  min-height: 44px;
  touch-action: manipulation;
}
```

IMPORTANT: Tailwind v4 uses CSS-native configuration with `@theme` directive, NOT `tailwind.config.js`. Do NOT create a JavaScript config file.
  </action>
  <verify>
    <automated>test -f src/styles/global.css && grep -q '@import.*tailwindcss' src/styles/global.css && grep -q '@theme' src/styles/global.css && grep -q 'color-card-light.*#F7F7F7' src/styles/global.css</automated>
  </verify>
  <done>
    - src/styles/global.css exists with Tailwind CSS import
    - @theme block contains all color tokens from UI-SPEC.md
    - Font families configured for Inter and JetBrains Mono
    - Spacing tokens are multiples of 4 as required
    - Base body styles apply Inter font and canvas white background
  </done>
</task>

<task type="auto">
  <name>Task 3: Create BaseLayout with font imports</name>
  <files>src/layouts/BaseLayout.astro</files>
  <read_first>
    - .planning/phases/01-foundation-bento-grid/01-UI-SPEC.md (for typography specs)
    - src/styles/global.css (created in Task 2, for style imports)
  </read_first>
  <behavior>
    - BaseLayout renders children correctly
    - Fonts are loaded from @fontsource packages
    - Global styles are applied
  </behavior>
  <action>
Create the base layout component that wraps all pages with fonts and global styles.

Create `src/layouts/BaseLayout.astro` with this exact content:
```astro
---
import '@fontsource/inter/300.css';
import '@fontsource/inter/400.css';
import '@fontsource/jetbrains-mono/300.css';
import '@fontsource/jetbrains-mono/400.css';
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
}

const {
  title = 'KERNEL_PANIC / ARCHITECTURE & SYSTEMS',
  description = 'Computing as craft — Technical writing and portfolio'
} = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>
  </head>
  <body class="min-h-screen bg-canvas-white text-ink-black">
    <slot />
  </body>
</html>

<style is:global>
  /* Ensure smooth scrolling */
  html {
    scroll-behavior: smooth;
  }

  /* Focus styles for accessibility */
  :focus-visible {
    outline: 2px solid var(--color-ink-black);
    outline-offset: 2px;
  }
</style>
```

This layout:
- Imports Inter (300, 400 weights) and JetBrains Mono (300, 400 weights) from @fontsource
- Applies global CSS with Tailwind theme
- Sets up basic meta tags (title, description, viewport)
- Provides slot for page content
- Adds smooth scrolling and focus styles for accessibility
  </action>
  <verify>
    <automated>test -f src/layouts/BaseLayout.astro && grep -q '@fontsource/inter' src/layouts/BaseLayout.astro && grep -q '@fontsource/jetbrains-mono' src/layouts/BaseLayout.astro && grep -q 'import.*global.css' src/layouts/BaseLayout.astro</automated>
  </verify>
  <done>
    - src/layouts/BaseLayout.astro exists with font imports
    - Inter 300, 400 and JetBrains Mono 300, 400 fonts imported
    - Global styles imported
    - Layout renders with proper HTML structure and meta tags
  </done>
</task>

<task type="auto">
  <name>Task 4: Set up test infrastructure (Vitest + Playwright)</name>
  <files>vitest.config.ts, playwright.config.ts, src/tests/setup.ts</files>
  <read_first>
    - .planning/phases/01-foundation-bento-grid/01-RESEARCH.md (for Vitest/Playwright configuration patterns)
  </read_first>
  <behavior>
    - Vitest runs unit tests successfully
    - Playwright runs e2e tests successfully
    - Test setup file configures testing utilities
  </behavior>
  <action>
Create test configuration files for Vitest (unit) and Playwright (e2e) testing.

1. Create `vitest.config.ts`:
```typescript
/// <reference types="vitest/config" />
import { getViteConfig } from 'astro/config';

export default getViteConfig({
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: ['./src/tests/setup.ts'],
    include: ['src/**/*.{test,spec}.{ts,tsx}'],
    coverage: {
      reporter: ['text', 'json', 'html'],
      exclude: [
        'node_modules/',
        'src/config/',
        'src/tests/'
      ]
    }
  }
});
```

2. Create `playwright.config.ts`:
```typescript
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './tests/e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: 'html',
  use: {
    baseURL: 'http://localhost:4321',
    trace: 'on-first-retry',
    screenshot: 'only-on-failure'
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] }
    },
    {
      name: 'webkit',
      use: { ...devices['Desktop Safari'] }
    },
    {
      name: 'Mobile Chrome',
      use: { ...devices['Pixel 5'] }
    }
  ],
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:4321',
    reuseExistingServer: !process.env.CI
  }
});
```

3. Create `src/tests/setup.ts`:
```typescript
import { expect, afterEach, vi } from 'vitest';
import { cleanup } from '@testing-library/react';

// Cleanup after each test
afterEach(() => {
  cleanup();
});

// Mock IntersectionObserver for components that use it
global.IntersectionObserver = vi.fn().mockImplementation(() => ({
  observe: vi.fn(),
  unobserve: vi.fn(),
  disconnect: vi.fn()
}));

// Mock matchMedia for responsive hooks
Object.defineProperty(window, 'matchMedia', {
  writable: true,
  value: vi.fn().mockImplementation(query => ({
    matches: false,
    media: query,
    onchange: null,
    addListener: vi.fn(),
    removeListener: vi.fn(),
    addEventListener: vi.fn(),
    removeEventListener: vi.fn(),
    dispatchEvent: vi.fn()
  }))
});
```

4. Create test directories:
```bash
mkdir -p tests/e2e src/tests/unit
```

5. Create a placeholder test file to verify setup - `src/tests/unit/example.test.ts`:
```typescript
import { describe, it, expect } from 'vitest';

describe('Test Infrastructure', () => {
  it('runs tests correctly', () => {
    expect(true).toBe(true);
  });
});
```
  </action>
  <verify>
    <automated>test -f vitest.config.ts && test -f playwright.config.ts && test -f src/tests/setup.ts && npm run test:unit 2>&1 | grep -q 'passed'</automated>
  </verify>
  <done>
    - vitest.config.ts exists with Astro's getViteConfig integration
    - playwright.config.ts exists with baseURL and browser projects
    - src/tests/setup.ts exists with React Testing Library cleanup
    - npm run test:unit executes successfully with at least 1 passing test
    - tests/e2e and src/tests/unit directories exist
  </done>
</task>

</tasks>

<verification>
Run these commands to verify the plan is complete:

1. **Dependencies installed:**
   ```bash
   npm list astro react tailwindcss vitest @playwright/test
   ```

2. **Dev server starts:**
   ```bash
   npm run dev &
   sleep 5
   curl -s http://localhost:4321 | head -20
   ```

3. **Build succeeds:**
   ```bash
   npm run build
   ```

4. **Tests run:**
   ```bash
   npm run test:unit
   ```
</verification>

<success_criteria>
- [ ] All dependencies installed without errors
- [ ] Dev server starts on http://localhost:4321
- [ ] Build generates output in `dist/` directory
- [ ] Unit tests execute and pass
- [ ] Tailwind CSS classes generate styles
- [ ] Fonts load correctly (Inter, JetBrains Mono)
- [ ] BaseLayout renders without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-bento-grid/01-01-SUMMARY.md`
</output>
