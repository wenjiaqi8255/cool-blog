---
phase: 01-foundation-bento-grid
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - src/components/layout/Header.astro
  - src/components/interactive/TabNavigation.tsx
  - src/components/interactive/SubscribeModal.tsx
  - src/pages/index.astro
  - src/pages/articles.astro
  - src/tests/unit/components/TabNavigation.test.tsx
  - tests/e2e/navigation.spec.ts
autonomous: true
requirements:
  - PORT-05
  - RESP-02
user_setup: []

must_haves:
  truths:
    - "User can see header with brand logo and navigation"
    - "User can click Articles tab to switch to Articles view"
    - "User can click Subscribe button to open modal"
    - "User can enter email in subscribe modal"
    - "All interactive elements have 44px minimum tap targets"
  artifacts:
    - path: "src/components/layout/Header.astro"
      provides: "Site header with navigation"
      contains: "KERNEL_PANIC"
    - path: "src/components/interactive/TabNavigation.tsx"
      provides: "Tab switching between Portfolio and Articles"
      contains: "useState"
    - path: "src/components/interactive/SubscribeModal.tsx"
      provides: "Newsletter subscription modal"
      contains: "email"
    - path: "src/pages/index.astro"
      provides: "Portfolio page"
      contains: "BentoGrid"
    - path: "src/pages/articles.astro"
      provides: "Articles page placeholder"
      contains: "Articles"
  key_links:
    - from: "Header.astro"
      to: "TabNavigation.tsx"
      via: "import and client:load"
      pattern: "TabNavigation"
    - from: "Header.astro"
      to: "SubscribeModal.tsx"
      via: "import and client:load"
      pattern: "SubscribeModal"
    - from: "TabNavigation.tsx"
      to: "index.astro / articles.astro"
      via: "Astro routing"
      pattern: "window.location"
---

<objective>
Build the navigation system with Header, TabNavigation, and SubscribeModal components. Create the main pages (Portfolio and Articles) that users navigate between. Ensure all interactive elements meet accessibility requirements (44px touch targets).

Purpose: Enable user navigation between Portfolio and Articles views, and provide newsletter subscription entry point.
Output: Working navigation with accessible touch targets, modal for email capture.
</objective>

<execution_context>
@/Users/wenjiaqi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenjiaqi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-bento-grid/01-CONTEXT.md
@.planning/phases/01-foundation-bento-grid/01-UI-SPEC.md
@.planning/phases/01-foundation-bento-grid/01-RESEARCH.md

<interfaces>
<!-- From Plan 03 - layout component -->

From src/components/layout/BentoGrid.astro (Plan 03):
- Usage: <BentoGrid cards={portfolioCards} />

From UI-SPEC.md - Copywriting:
- Header brand: "KERNEL_PANIC / ARCHITECTURE & SYSTEMS"
- Articles tab: "Articles"
- Subscribe button: "Subscribe"
- Subscribe modal heading: "Subscribe to the newsletter"
- Subscribe modal body: "Get the latest articles and updates delivered to your inbox."
- Subscribe modal input placeholder: "your@email.com"

From CONTEXT.md - Navigation Decisions:
- Header links: Only "Articles" tab - remove Setups/Repo from nav
- Articles tab switches view to Articles list (Phase 2 will populate)
- Subscribe button: Opens modal overlay with email capture form

From UI-SPEC.md - Touch Targets:
- 44px minimum on all interactive elements (RESP-02)
- touch-action: manipulation to prevent zoom
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Header component with navigation</name>
  <files>src/components/layout/Header.astro</files>
  <read_first>
    - .planning/phases/01-foundation-bento-grid/01-UI-SPEC.md (for header specs and copywriting)
    - .planning/phases/01-foundation-bento-grid/01-CONTEXT.md (for navigation requirements)
  </read_first>
  <behavior>
    - Header displays brand name "KERNEL_PANIC / ARCHITECTURE & SYSTEMS"
    - Header includes Articles tab button
    - Header includes Subscribe button
    - All buttons have 44px minimum touch targets
  </behavior>
  <action>
Create the Header component with brand, navigation, and subscribe button.

Create `src/components/layout/Header.astro`:
```astro
---
import TabNavigation from '../interactive/TabNavigation';
import SubscribeModal from '../interactive/SubscribeModal';

interface Props {
  currentPath?: string;
}

const { currentPath = '/' } = Astro.props;
---

<header class="header">
  <div class="header-content">
    <a href="/" class="brand">
      <span class="brand-text">KERNEL_PANIC</span>
      <span class="brand-divider">/</span>
      <span class="brand-subtitle">ARCHITECTURE & SYSTEMS</span>
    </a>

    <nav class="nav">
      <TabNavigation client:load currentPath={currentPath} />
      <SubscribeModal client:load />
    </nav>
  </div>
</header>

<style>
  .header {
    position: sticky;
    top: 0;
    z-index: 100;
    background: var(--color-canvas-white);
    border-bottom: 1px solid #EEEEEE;
  }

  .header-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    max-width: 1600px;
    margin: 0 auto;
    padding: 20px 40px;
    gap: 24px;
  }

  .brand {
    display: flex;
    align-items: baseline;
    gap: 8px;
    text-decoration: none;
    color: inherit;
    flex-shrink: 0;
  }

  .brand-text {
    font-size: 16px;
    font-weight: 600;
    letter-spacing: -0.02em;
    color: var(--color-ink-black);
  }

  .brand-divider {
    font-size: 16px;
    color: var(--color-ink-light);
    margin: 0 4px;
  }

  .brand-subtitle {
    font-size: 12px;
    font-weight: 400;
    letter-spacing: 0.05em;
    color: var(--color-ink-gray);
    display: none;
  }

  @media (min-width: 768px) {
    .brand-subtitle {
      display: inline;
    }
  }

  .nav {
    display: flex;
    align-items: center;
    gap: 16px;
  }

  /* Mobile: Stack header vertically */
  @media (max-width: 640px) {
    .header-content {
      flex-direction: column;
      align-items: flex-start;
      padding: 16px;
      gap: 16px;
    }

    .nav {
      width: 100%;
      justify-content: space-between;
    }
  }
</style>
```
  </action>
  <verify>
    <automated>test -f src/components/layout/Header.astro && grep -q 'KERNEL_PANIC' src/components/layout/Header.astro && grep -q 'TabNavigation' src/components/layout/Header.astro && grep -q 'SubscribeModal' src/components/layout/Header.astro && grep -q 'client:load' src/components/layout/Header.astro</automated>
  </verify>
  <done>
    - Header.astro exists with brand name
    - TabNavigation imported with client:load
    - SubscribeModal imported with client:load
    - Responsive layout (stacked on mobile)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create TabNavigation component</name>
  <files>src/components/interactive/TabNavigation.tsx, src/tests/unit/components/TabNavigation.test.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-bento-grid/01-UI-SPEC.md (for copywriting)
    - .planning/phases/01-foundation-bento-grid/01-CONTEXT.md (for navigation behavior)
  </read_first>
  <behavior>
    - TabNavigation shows "Articles" tab button
    - Clicking Articles navigates to /articles page
    - Current page tab is highlighted
    - Tab buttons have 44px minimum touch targets
  </behavior>
  <action>
Create the TabNavigation React component for switching between Portfolio and Articles.

Create `src/components/interactive/TabNavigation.tsx`:
```tsx
import { useEffect, useState } from 'react';

interface Props {
  currentPath?: string;
}

export default function TabNavigation({ currentPath: initialPath }: Props) {
  const [currentPath, setCurrentPath] = useState(initialPath || '/');

  // Update current path on client-side navigation
  useEffect(() => {
    setCurrentPath(window.location.pathname);
  }, []);

  const tabs = [
    { label: 'Portfolio', href: '/' },
    { label: 'Articles', href: '/articles' }
  ];

  return (
    <nav className="tab-navigation">
      {tabs.map((tab) => {
        const isActive = currentPath === tab.href;
        return (
          <a
            key={tab.href}
            href={tab.href}
            className={`tab-button ${isActive ? 'active' : ''}`}
            aria-current={isActive ? 'page' : undefined}
          >
            {tab.label}
          </a>
        );
      })}
    </nav>
  );
}
```

Add styles to `src/styles/global.css` (append):
```css
/* Tab Navigation styles */
.tab-navigation {
  display: flex;
  gap: 8px;
}

.tab-button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 44px;
  min-width: 44px;
  padding: 8px 16px;
  font-size: 14px;
  font-weight: 400;
  color: var(--color-ink-gray);
  background: transparent;
  border: 1px solid var(--color-ink-black);
  border-radius: 9999px;
  text-decoration: none;
  transition: all 200ms cubic-bezier(0.4, 0, 0.2, 1);
  touch-action: manipulation;
}

.tab-button:hover {
  background: var(--color-card-light);
  color: var(--color-ink-black);
}

.tab-button.active {
  background: var(--color-ink-black);
  color: #FFFFFF;
  border-color: var(--color-ink-black);
}

.tab-button:focus-visible {
  outline: 2px solid var(--color-ink-black);
  outline-offset: 2px;
}

/* Subscribe Button styles */
.subscribe-button {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 44px;
  min-width: 44px;
  padding: 8px 20px;
  font-size: 14px;
  font-weight: 500;
  color: #FFFFFF;
  background: var(--color-ink-black);
  border: 1px solid var(--color-ink-black);
  border-radius: 9999px;
  cursor: pointer;
  text-decoration: none;
  transition: all 200ms cubic-bezier(0.4, 0, 0.2, 1);
  touch-action: manipulation;
}

.subscribe-button:hover {
  background: var(--color-dark-card-hover);
  border-color: var(--color-dark-card-hover);
}

.subscribe-button:focus-visible {
  outline: 2px solid var(--color-ink-black);
  outline-offset: 2px;
}

/* Subscribe Modal styles */
.modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
  padding: 16px;
}

.modal-content {
  background: #FFFFFF;
  border-radius: 12px;
  padding: 32px;
  max-width: 400px;
  width: 100%;
  box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
}

.modal-header {
  margin-bottom: 24px;
}

.modal-title {
  font-size: 24px;
  font-weight: 300;
  letter-spacing: -0.02em;
  color: var(--color-ink-black);
  margin: 0 0 8px 0;
}

.modal-description {
  font-size: 14px;
  color: var(--color-ink-gray);
  margin: 0;
  line-height: 1.5;
}

.modal-form {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.modal-input {
  width: 100%;
  min-height: 44px;
  padding: 12px 16px;
  font-size: 16px;
  border: 1px solid #E5E5E5;
  border-radius: 8px;
  background: #FFFFFF;
  color: var(--color-ink-black);
  transition: border-color 200ms;
}

.modal-input:focus {
  outline: none;
  border-color: var(--color-ink-black);
}

.modal-input::placeholder {
  color: var(--color-ink-light);
}

.modal-submit {
  width: 100%;
  min-height: 44px;
  padding: 12px 24px;
  font-size: 14px;
  font-weight: 500;
  color: #FFFFFF;
  background: var(--color-ink-black);
  border: none;
  border-radius: 8px;
  cursor: pointer;
  transition: background-color 200ms;
  touch-action: manipulation;
}

.modal-submit:hover {
  background: var(--color-dark-card-hover);
}

.modal-submit:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.modal-close {
  position: absolute;
  top: 16px;
  right: 16px;
  width: 44px;
  height: 44px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: transparent;
  border: none;
  cursor: pointer;
  color: var(--color-ink-gray);
  font-size: 24px;
  touch-action: manipulation;
}

.modal-close:hover {
  color: var(--color-ink-black);
}
```

Create test file `src/tests/unit/components/TabNavigation.test.tsx`:
```tsx
import { describe, it, expect, vi } from 'vitest';
import { render, screen } from '@testing-library/react';
import TabNavigation from '../../../components/interactive/TabNavigation';

// Mock window.location
Object.defineProperty(window, 'location', {
  value: {
    pathname: '/',
    href: 'http://localhost/'
  },
  writable: true
});

describe('TabNavigation', () => {
  it('renders Portfolio and Articles tabs', () => {
    render(<TabNavigation currentPath="/" />);

    expect(screen.getByText('Portfolio')).toBeInTheDocument();
    expect(screen.getByText('Articles')).toBeInTheDocument();
  });

  it('highlights current page tab', () => {
    render(<TabNavigation currentPath="/" />);

    const portfolioTab = screen.getByText('Portfolio');
    expect(portfolioTab).toHaveClass('active');
  });

  it('Articles tab is active on /articles page', () => {
    render(<TabNavigation currentPath="/articles" />);

    const articlesTab = screen.getByText('Articles');
    expect(articlesTab).toHaveClass('active');
  });

  it('tabs have correct href attributes', () => {
    render(<TabNavigation currentPath="/" />);

    const portfolioLink = screen.getByText('Portfolio').closest('a');
    const articlesLink = screen.getByText('Articles').closest('a');

    expect(portfolioLink).toHaveAttribute('href', '/');
    expect(articlesLink).toHaveAttribute('href', '/articles');
  });
});
```
  </action>
  <verify>
    <automated>test -f src/components/interactive/TabNavigation.tsx && grep -q 'useState' src/components/interactive/TabNavigation.tsx && grep -q 'Articles' src/components/interactive/TabNavigation.tsx && test -f src/tests/unit/components/TabNavigation.test.tsx && npm run test:unit -- --filter=TabNavigation 2>&1 | grep -q 'passed'</automated>
  </verify>
  <done>
    - TabNavigation.tsx exists with Portfolio and Articles tabs
    - Active tab highlighting works
    - Correct href attributes
    - Unit tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Create SubscribeModal component</name>
  <files>src/components/interactive/SubscribeModal.tsx</files>
  <read_first>
    - .planning/phases/01-foundation-bento-grid/01-UI-SPEC.md (for modal copywriting)
    - .planning/phases/01-foundation-bento-grid/01-CONTEXT.md (for modal behavior)
  </read_first>
  <behavior>
    - SubscribeModal shows Subscribe button
    - Clicking Subscribe opens modal overlay
    - Modal has email input with placeholder
    - Modal has submit button
    - Modal closes on clicking outside or close button
    - All interactive elements have 44px touch targets
  </behavior>
  <action>
Create the SubscribeModal React component with email capture form.

Create `src/components/interactive/SubscribeModal.tsx`:
```tsx
import { useState, useEffect, useRef } from 'react';

export default function SubscribeModal() {
  const [isOpen, setIsOpen] = useState(false);
  const [email, setEmail] = useState('');
  const [isSubmitting, setIsSubmitting] = useState(false);
  const modalRef = useRef<HTMLDivElement>(null);

  // Close on Escape key
  useEffect(() => {
    const handleEscape = (e: KeyboardEvent) => {
      if (e.key === 'Escape' && isOpen) {
        setIsOpen(false);
      }
    };

    document.addEventListener('keydown', handleEscape);
    return () => document.removeEventListener('keydown', handleEscape);
  }, [isOpen]);

  // Lock body scroll when modal is open
  useEffect(() => {
    if (isOpen) {
      document.body.style.overflow = 'hidden';
    } else {
      document.body.style.overflow = '';
    }
    return () => {
      document.body.style.overflow = '';
    };
  }, [isOpen]);

  // Focus trap
  useEffect(() => {
    if (isOpen && modalRef.current) {
      const focusableElements = modalRef.current.querySelectorAll(
        'button, input, [tabindex]:not([tabindex="-1"])'
      );
      const firstElement = focusableElements[0] as HTMLElement;
      const lastElement = focusableElements[focusableElements.length - 1] as HTMLElement;

      const handleTab = (e: KeyboardEvent) => {
        if (e.key !== 'Tab') return;

        if (e.shiftKey) {
          if (document.activeElement === firstElement) {
            e.preventDefault();
            lastElement?.focus();
          }
        } else {
          if (document.activeElement === lastElement) {
            e.preventDefault();
            firstElement?.focus();
          }
        }
      };

      document.addEventListener('keydown', handleTab);
      firstElement?.focus();

      return () => document.removeEventListener('keydown', handleTab);
    }
  }, [isOpen]);

  const handleOpen = () => setIsOpen(true);

  const handleClose = () => {
    setIsOpen(false);
    setEmail('');
  };

  const handleOverlayClick = (e: React.MouseEvent) => {
    if (e.target === e.currentTarget) {
      handleClose();
    }
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();

    if (!email || isSubmitting) return;

    setIsSubmitting(true);

    // Phase 3: Will submit to backend API
    // For now, just log and close
    console.log('Newsletter subscription:', email);

    // Simulate API call
    await new Promise((resolve) => setTimeout(resolve, 500));

    setIsSubmitting(false);
    handleClose();

    // Show success message (Phase 3 will implement properly)
    alert('Thanks for subscribing! (Demo mode - backend coming in Phase 3)');
  };

  return (
    <>
      <button
        onClick={handleOpen}
        className="subscribe-button"
        aria-haspopup="dialog"
      >
        Subscribe
      </button>

      {isOpen && (
        <div
          className="modal-overlay"
          onClick={handleOverlayClick}
          role="dialog"
          aria-modal="true"
          aria-labelledby="modal-title"
        >
          <div className="modal-content" ref={modalRef}>
            <button
              onClick={handleClose}
              className="modal-close"
              aria-label="Close modal"
            >
              ×
            </button>

            <div className="modal-header">
              <h2 id="modal-title" className="modal-title">
                Subscribe to the newsletter
              </h2>
              <p className="modal-description">
                Get the latest articles and updates delivered to your inbox.
              </p>
            </div>

            <form onSubmit={handleSubmit} className="modal-form">
              <input
                type="email"
                value={email}
                onChange={(e) => setEmail(e.target.value)}
                placeholder="your@email.com"
                className="modal-input"
                required
                disabled={isSubmitting}
                aria-label="Email address"
              />

              <button
                type="submit"
                className="modal-submit"
                disabled={isSubmitting || !email}
              >
                {isSubmitting ? 'Subscribing...' : 'Subscribe'}
              </button>
            </form>
          </div>
        </div>
      )}
    </>
  );
}
```

Note: Phase 3 will connect this to the backend API. For now, it logs and shows a demo message.
  </action>
  <verify>
    <automated>test -f src/components/interactive/SubscribeModal.tsx && grep -q 'useState' src/components/interactive/SubscribeModal.tsx && grep -q 'your@email.com' src/components/interactive/SubscribeModal.tsx && grep -q 'Subscribe to the newsletter' src/components/interactive/SubscribeModal.tsx</automated>
  </verify>
  <done>
    - SubscribeModal.tsx exists with email capture
    - Opens on Subscribe button click
    - Closes on Escape key and overlay click
    - Focus trap implemented
    - Body scroll locked when open
    - 44px touch targets on all interactive elements
  </done>
</task>

<task type="auto">
  <name>Task 4: Create Portfolio and Articles pages</name>
  <files>src/pages/index.astro, src/pages/articles.astro</files>
  <read_first>
    - src/layouts/BaseLayout.astro (from Plan 01)
    - src/components/layout/Header.astro (from Task 1)
    - src/components/layout/BentoGrid.astro (from Plan 03)
  </read_first>
  <behavior>
    - index.astro renders Portfolio view with BentoGrid
    - articles.astro renders Articles placeholder
    - Both pages use BaseLayout
    - Both pages include Header with correct currentPath
  </behavior>
  <action>
Create the main pages for Portfolio and Articles views.

1. Create `src/pages/index.astro`:
```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/layout/Header.astro';
import BentoGrid from '../components/layout/BentoGrid.astro';

const currentPath = Astro.url.pathname;
---

<BaseLayout
  title="KERNEL_PANIC / ARCHITECTURE & SYSTEMS"
  description="Computing as craft — Technical writing and portfolio"
>
  <Header currentPath={currentPath} />
  <main class="main-content">
    <BentoGrid />
  </main>
</BaseLayout>

<style>
  .main-content {
    padding-top: 24px;
    padding-bottom: 64px;
  }
</style>
```

2. Create `src/pages/articles.astro`:
```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/layout/Header.astro';

const currentPath = Astro.url.pathname;
---

<BaseLayout
  title="Articles — KERNEL_PANIC"
  description="Technical articles on machine learning, systems engineering, and software craftsmanship"
>
  <Header currentPath={currentPath} />
  <main class="main-content">
    <div class="articles-container">
      <h1 class="page-title">Articles</h1>
      <p class="page-description">
        Deep dives into machine learning, distributed systems, and the craft of building robust software.
      </p>

      <div class="placeholder-state">
        <div class="placeholder-icon">📝</div>
        <h2 class="placeholder-title">Articles Coming Soon</h2>
        <p class="placeholder-text">
          Phase 2 will populate this page with technical articles and tutorials.
        </p>
      </div>
    </div>
  </main>
</BaseLayout>

<style>
  .main-content {
    padding-top: 24px;
    padding-bottom: 64px;
  }

  .articles-container {
    max-width: 1600px;
    margin: 0 auto;
    padding: 0 40px;
  }

  .page-title {
    font-size: 48px;
    font-weight: 200;
    letter-spacing: -0.04em;
    color: var(--color-ink-black);
    margin: 0 0 16px 0;
  }

  .page-description {
    font-size: 18px;
    color: var(--color-ink-gray);
    margin: 0 0 48px 0;
    max-width: 600px;
  }

  .placeholder-state {
    text-align: center;
    padding: 80px 24px;
    background: var(--color-card-light);
    border-radius: 12px;
  }

  .placeholder-icon {
    font-size: 48px;
    margin-bottom: 16px;
  }

  .placeholder-title {
    font-size: 24px;
    font-weight: 300;
    letter-spacing: -0.02em;
    color: var(--color-ink-black);
    margin: 0 0 8px 0;
  }

  .placeholder-text {
    font-size: 14px;
    color: var(--color-ink-gray);
    margin: 0;
  }

  @media (max-width: 768px) {
    .articles-container {
      padding: 0 16px;
    }

    .page-title {
      font-size: 32px;
    }

    .page-description {
      font-size: 16px;
    }
  }
</style>
```
  </action>
  <verify>
    <automated>test -f src/pages/index.astro && test -f src/pages/articles.astro && grep -q 'BentoGrid' src/pages/index.astro && grep -q 'Articles Coming Soon' src/pages/articles.astro && grep -q 'currentPath' src/pages/index.astro</automated>
  </verify>
  <done>
    - src/pages/index.astro exists with BentoGrid
    - src/pages/articles.astro exists with placeholder
    - Both pages use BaseLayout and Header
    - currentPath passed to Header for active tab highlighting
  </done>
</task>

<task type="auto">
  <name>Task 5: Create E2E tests for navigation</name>
  <files>tests/e2e/navigation.spec.ts</files>
  <read_first>
    - .planning/phases/01-foundation-bento-grid/01-RESEARCH.md (for Playwright patterns)
    - src/components/interactive/TabNavigation.tsx (from Task 2)
  </read_first>
  <behavior>
    - Test verifies tab navigation between Portfolio and Articles
    - Test verifies subscribe modal opens and closes
    - Test verifies touch targets are 44px minimum
  </behavior>
  <action>
Create Playwright E2E tests for navigation and accessibility.

Create `tests/e2e/navigation.spec.ts`:
```typescript
import { test, expect } from '@playwright/test';

test.describe('Navigation', () => {
  test('Portfolio tab is active on home page', async ({ page }) => {
    await page.goto('/');

    const portfolioTab = page.getByRole('link', { name: 'Portfolio' });
    await expect(portfolioTab).toHaveClass(/active/);
  });

  test('Articles tab is active on articles page', async ({ page }) => {
    await page.goto('/articles');

    const articlesTab = page.getByRole('link', { name: 'Articles' });
    await expect(articlesTab).toHaveClass(/active/);
  });

  test('clicking Articles tab navigates to articles page', async ({ page }) => {
    await page.goto('/');

    const articlesTab = page.getByRole('link', { name: 'Articles' });
    await articlesTab.click();

    await expect(page).toHaveURL('/articles');
  });

  test('clicking Portfolio tab navigates to home page', async ({ page }) => {
    await page.goto('/articles');

    const portfolioTab = page.getByRole('link', { name: 'Portfolio' });
    await portfolioTab.click();

    await expect(page).toHaveURL('/');
  });
});

test.describe('Subscribe Modal', () => {
  test('opens subscribe modal on button click', async ({ page }) => {
    await page.goto('/');

    const subscribeButton = page.getByRole('button', { name: 'Subscribe' });
    await subscribeButton.click();

    const modal = page.getByRole('dialog');
    await expect(modal).toBeVisible();

    const modalTitle = page.getByText('Subscribe to the newsletter');
    await expect(modalTitle).toBeVisible();
  });

  test('closes modal on close button click', async ({ page }) => {
    await page.goto('/');

    // Open modal
    const subscribeButton = page.getByRole('button', { name: 'Subscribe' });
    await subscribeButton.click();

    // Close modal
    const closeButton = page.getByRole('button', { name: 'Close modal' });
    await closeButton.click();

    const modal = page.getByRole('dialog');
    await expect(modal).not.toBeVisible();
  });

  test('closes modal on Escape key', async ({ page }) => {
    await page.goto('/');

    // Open modal
    const subscribeButton = page.getByRole('button', { name: 'Subscribe' });
    await subscribeButton.click();

    // Press Escape
    await page.keyboard.press('Escape');

    const modal = page.getByRole('dialog');
    await expect(modal).not.toBeVisible();
  });

  test('closes modal on overlay click', async ({ page }) => {
    await page.goto('/');

    // Open modal
    const subscribeButton = page.getByRole('button', { name: 'Subscribe' });
    await subscribeButton.click();

    // Click overlay (outside modal content)
    const overlay = page.locator('.modal-overlay');
    await overlay.click({ position: { x: 10, y: 10 } });

    const modal = page.getByRole('dialog');
    await expect(modal).not.toBeVisible();
  });

  test('email input has correct placeholder', async ({ page }) => {
    await page.goto('/');

    const subscribeButton = page.getByRole('button', { name: 'Subscribe' });
    await subscribeButton.click();

    const emailInput = page.getByPlaceholder('your@email.com');
    await expect(emailInput).toBeVisible();
  });
});

test.describe('Touch Targets (Accessibility)', () => {
  test('all interactive elements have 44px minimum touch targets', async ({ page }) => {
    await page.setViewportSize({ width: 375, height: 667 });
    await page.goto('/');

    // Check tab buttons
    const tabButtons = page.locator('.tab-button');
    const tabCount = await tabButtons.count();

    for (let i = 0; i < tabCount; i++) {
      const button = tabButtons.nth(i);
      const box = await button.boundingBox();
      expect(box?.height).toBeGreaterThanOrEqual(44);
      expect(box?.width).toBeGreaterThanOrEqual(44);
    }

    // Check subscribe button
    const subscribeButton = page.getByRole('button', { name: 'Subscribe' });
    const subscribeBox = await subscribeButton.boundingBox();
    expect(subscribeBox?.height).toBeGreaterThanOrEqual(44);
    expect(subscribeBox?.width).toBeGreaterThanOrEqual(44);

    // Open modal and check modal elements
    await subscribeButton.click();

    const emailInput = page.getByPlaceholder('your@email.com');
    const inputBox = await emailInput.boundingBox();
    expect(inputBox?.height).toBeGreaterThanOrEqual(44);

    const submitButton = page.locator('.modal-submit');
    const submitBox = await submitButton.boundingBox();
    expect(submitBox?.height).toBeGreaterThanOrEqual(44);

    const closeButton = page.getByRole('button', { name: 'Close modal' });
    const closeBox = await closeButton.boundingBox();
    expect(closeBox?.height).toBeGreaterThanOrEqual(44);
    expect(closeBox?.width).toBeGreaterThanOrEqual(44);
  });
});
```
  </action>
  <verify>
    <automated>test -f tests/e2e/navigation.spec.ts && grep -q "Navigation" tests/e2e/navigation.spec.ts && grep -q "Subscribe Modal" tests/e2e/navigation.spec.ts && grep -q "Touch Targets" tests/e2e/navigation.spec.ts && grep -q "44" tests/e2e/navigation.spec.ts</automated>
  </verify>
  <done>
    - tests/e2e/navigation.spec.ts exists
    - Tests for tab navigation
    - Tests for subscribe modal (open, close, escape, overlay)
    - Tests for 44px touch targets on all interactive elements
  </done>
</task>

</tasks>

<verification>
Run these commands to verify the plan is complete:

1. **All components exist:**
   ```bash
   ls -la src/components/layout/Header.astro
   ls -la src/components/interactive/TabNavigation.tsx
   ls -la src/components/interactive/SubscribeModal.tsx
   ```

2. **Pages exist:**
   ```bash
   ls -la src/pages/index.astro src/pages/articles.astro
   ```

3. **Unit tests pass:**
   ```bash
   npm run test:unit
   ```

4. **E2E tests exist:**
   ```bash
   ls -la tests/e2e/navigation.spec.ts
   ```
</verification>

<success_criteria>
- [ ] Header displays brand name correctly
- [ ] TabNavigation switches between Portfolio and Articles
- [ ] SubscribeModal opens and closes correctly
- [ ] All interactive elements have 44px touch targets
- [ ] Focus trap works in modal
- [ ] Escape key closes modal
- [ ] Body scroll locked when modal open
- [ ] Both pages render without errors
- [ ] Unit and E2E tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-bento-grid/01-04-SUMMARY.md`
</output>
